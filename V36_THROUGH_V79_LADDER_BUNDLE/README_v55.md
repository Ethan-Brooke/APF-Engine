# APF v55 — Global Trace-to-Scheme Closure Registry

This package extends v54 and consolidates v35--v54 into one status registry.

Main claim: all current sector statuses are now recorded in a single claim ladder, with allowed claims and forbidden overclaims.

Targeted verifier: `scripts/check_global_trace_to_scheme_closure_registry_v55.py`.
