# APF v40 Charged-Lepton Trace-Vector Source Gate and QED Codomain

Status: `CHARGED_LEPTON_TRACE_VECTOR_SOURCE_GATE_PASS`.

v40 does not claim charged-lepton physical export. It closes the next honest obstruction layer:

- pole codomain can be declared as the first comparison codomain (`S = pole`, `Z_l^pole = I` in minimal bookkeeping);
- the numeric APF trace vector `T_l` remains the primary source gate;
- supplying `T_l` is equivalent to closing the lepton cooperative-overlap content `Omega_l` or replacing it with another APF-native trace-sector theorem;
- inverse construction from `(m_e,m_mu,m_tau)` is explicitly forbidden.

Current charged-lepton status: `P_source_gate^T_l + P_codomain^pole_ready`, not `P_export_candidate`.

Next theorem: `APF_LEPTON_COOPERATIVE_OVERLAP_OR_TRACE_VECTOR_CLOSURE_THEOREM`.
