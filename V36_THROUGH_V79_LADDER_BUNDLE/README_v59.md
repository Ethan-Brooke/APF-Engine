# APF v59 — Top MSR Full QCD Conversion Ledger

This package extends v58 by closing the full external-ledger layer for the top-quark MSR route.

Main file:

- `paper/TOP_MSR_FULL_QCD_CONVERSION_LEDGER_v59.tex`

Targeted verifier:

- `scripts/check_top_msr_full_qcd_conversion_ledger_v59.py`

Closed claim:

```tex
t:[P_{full QCD conversion ledger complete}/P_{numeric final blocked by evaluator}]
```

This is not a full numeric top final. It is the complete ledger and evaluator specification needed before a defensible multi-loop numeric conversion can be run.
