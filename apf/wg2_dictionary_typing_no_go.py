"""The dictionary typing no-go: the w-propto-g^n index is content-blind; the M_cross gate is index-conditional.

v24.3.364 (2026-07-03, Wall B first dig; principal-ruled "bank nogo, proceed";
walker-spec'd + fresh-context hostile audit LAND-WITH-FIXES 0.87, all eight
fixes carried C1/C2/R1-R6). Walk of record:
"Dictionary_Typing_Walk_2026-07-03.md" (The Turning/, with the executable
witness dictionary_typing_walk_witness.py). Instrument: the .354/.359
record-partition (apf/strong_cp_completion_no_go.py), CALLED LIVE in-body --
not mirrored (audit fix R1).

THE QUESTION. The ledger share sin^2 theta_W = 3/13 is [P]
(T_ew_load_placement_P). Reading it as the MEASURED weak mixing angle crosses
the w propto g^2 dictionary -- the fence that holds T_sin2theta (and
its seam family) at [P_structural_seam]; T24 was re-scoped to the value
claim [P] (2026-07-03 split). Until this pass the fence stood by ruling ("reserved at
[P_structural] by design", codomain_transport_schema.py). This module replaces
the ruling with a theorem-shaped ceiling, exactly the relation the .359 no-go
bears to NRDT:

  CHECK 1 (check_L_wg2_dictionary_index_content_blind_native). Written as a
  functional-index family w propto g^n (admissible completions = positive,
  order-preserving maps from the demand register to the realized coupling
  register), the selecting index n is partition-shaped and types NO-RECORD:
  maintaining any n yields zero native capacity gain (C_total = 61 for every
  probed n). The index is CONTENT-BLIND: completions R2 (n=2) and R4 (n=4) of
  the same banked native content share EVERY native-preserved invariant
  (C_total, d_eff, sigma, gamma = (1, 17/4), the share 3/13, the one-loop
  beta counts, the crossing values 47.02 and 61) yet realize distinct angles
  (3/13 vs ~0.35389) -- the map (native data -> index) is not single-valued.
  Negative control: the ledger SHARE is content-FIXED (four placements, four
  pairwise-distinct native fractions 13/35, 3/13, 5/43, 13/19) -- the no-go
  lands exactly on the seam-crossing index, not as blanket skepticism. The
  scale-placement leg (cut 1) runs the same computation on the endpoint: the
  realized one-loop angle moves with the endpoint while the native vector
  stays fixed, so the endpoint placement is content-blind too; under the
  alternative typing (cut 2: instrumented transport) the promotion arithmetic
  is the same -- the ceiling is cut-robust.

  CHECK 2 (check_T_mcross_gate_dictionary_conditional). The banked forward
  solve (_alpha_s_forward, apf/abelian_coupling_capacity_count.py) DOES supply
  t = ln(M_cross/M_Z) = 34.59 from the counts -- the fence text's "which the
  counts do not supply" was half-stale as absolute text -- BUT the supplied t
  is dictionary-index-conditional in its very formulation: the ratio condition
  is stated in coupling space, and on IDENTICAL native inputs t(n) spans
  12.4 - 69.0 across the probed family (factor ~5.6). So the M_cross scale
  gate cannot discharge the scale seam independently of the dictionary: the
  scale seam is NESTED inside the dictionary seam -- the fence is one wall,
  not two -- and the promotion clause "a future [P] promotion must come from
  the M_cross scale gate" is foreclosed for bare [P]: a FULLY closed gate
  (.284 exhaustiveness closed AND the absolute-scale anchor closed) still
  yields t at [P]-given-the-dictionary plus an imported transport, capping the
  measured-angle reading at [P_structural_instrument], never theorem-[P].

  CHECK 3 (check_T_wg2_dictionary_index_no_native_consumer_census,
  v24.3.394, 2026-07-04 -- THE C(n) CONSTRUCTOR CENSUS; fresh-context walker
  CERTIFIED NEGATIVE 0.88 + fresh-context hostile audit LAND-WITH-FIXES 0.85,
  all fixes carried F1 MAJOR / F2 / F3 / F4 + notes F5 / F6; walk of record
  "The Turning/cn_constructor_census_walk_2026-07-04/"). The falsifier below
  carried the clause "none is visible; an objector must exhibit one" -- a
  visibility claim. Check 3 upgrades it to an inventory-certified verdict:
  over the FULL bank registry at the census tree (closed-world pin: landed
  at tree v24.3.394, EXPECTED_REGISTRY_SIZE 3880), NO banked constructor
  consumes the index. Three coordinated levels, in-check: (V) the native
  invariant vector, built by this module's own constructor with n live in
  scope, is identical across the probed family -- native capacity 61 at
  every n; (T) the reading-functional census's audited (Phi, D, nu)
  inventory carries exactly ONE index-typed row and it is this module's
  fence row -- the DX-uniqueness gate, honestly CURATION-DEPENDENT (audit
  F4): it nets a future consumer only if that consumer is curated into the
  rows; forward coverage is carried by the source tripwire, not this gate;
  (S) a package-recursive NINE-fingerprint source sweep -- a surface
  containing the defining file of every registered check, containment
  certified against the import-safe manifest module lists -- returns
  exactly the pinned disposition map of fence and fenced-reading surfaces,
  none a native-capacity constructor. The home module is EXCLUDED from the
  count map (self-hit stability) and guarded structurally instead by a
  pinned set of top-level callables (audit F3): a constructor added inside
  this module fails the pin until re-dispositioned. HONESTY CLAUSE (audit
  F1, MAJOR): the one known implicit member -- the hardwired n=2 ratio
  (1-s)/s in abelian_coupling_capacity_count.py -- fires ZERO of the eight
  vocabulary fingerprints and was dispositioned by PRIOR LANE KNOWLEDGE
  (.364), NOT caught by any census level; the NINTH fingerprint exists
  precisely to carry that disposition mechanized, and the named residual is
  broadened accordingly: a constructor riding a FIXED index presents as a
  constant and is invisible to all three levels (the demonstrated
  hardwired-index shape), alongside vocabulary-avoiding consumers and
  non-.py data payloads. The Level-T and Level-S probe regexes are pinned
  as DELIBERATELY DISTINCT instruments (audit F2): T probes the typed prose
  fields of a curated inventory, S probes source-code registers; each is
  tuned to its surface and unifying them would blunt both. Realized-side
  quantities that ARE functions of n (t, the realized angles, the M_cross
  composition) are dispositioned by TYPE, witnessed by check 2 run live:
  their formulation is stated in coupling space; their n-dependence is the
  WALL, not a consumer. Negative controls: the ledger share is
  content-FIXED (four placements, four distinct fractions, live from the
  [P] chain's own artifact); a SEEDED synthetic consumer is caught at both
  the value and the source level (the predicate is falsifiable, not
  vacuous-by-design); delta_CKM and the count-exponent C_total in
  d_eff^C_total return CONSUMED under the same predicates (the predicate
  discriminates). Grade [P_structural], the .355/.364 census/instrument
  family, bank-closed-world, conditional on the .354/.359 partition (called
  live); the strength disanalogy with .359 is inherited unchanged --
  inventory-certified CONSTRUCTION-LEVEL absence, not slot-impossibility.
  Sandbox-safe by construction: the census reads FILES (no registry
  import), and every live anchor is scipy-free.

THE EXCLUSION, RE-SCOPED (audit fix R3). Why does the native invariant vector
exclude the index? Two banked facts, jointly: (i) the [P] ledger chain to
3/13 is COUPLING-SYMBOL-FREE -- exact rationals end to end, no g anywhere
(recomputed in-body; witnessed against check_T_ew_load_placement_P's own
arithmetic); (ii) no native register discriminates the INDEX -- the crossing
values 47.02 and 61 enter as native counts, and their reading AS couplings
(T20 / L_coupling_capacity_id) is a distinct banked identification HELD FIXED
across the family, so it cannot select n. The strength disanalogy with .359,
stated honestly: there the exclusion is a slot-forbidding lemma
(L_anomaly_nonpert: no native angle slot exists); here it is a
construction-level absence (no constructor of the native vector consumes the
index). That is weaker in kind, and the grade carries it: this check
certifies a structural negative over banked inputs, conditional on the
.354/.359 partition -- it does not claim slot-impossibility.

THE PINNING, FAMILY-SCOPED (audit fix R6). The realized index is empirically
pinned: n = 2 is the unique survivor of the probed power family {1, 2, 4}
under the same three-output forward discrimination that priced the rival
placements; exact n = 2 is corroborated at the discriminator's grade;
family-exhaustiveness is its own open. This changes nothing about the typing
(content-blindness is a statement about the NATIVE map); every empirical row
lives in artifacts only, never in a pass gate (audit fix R2).

GRADE. Both checks [P_structural] tier 4, the .359 grade shape: typing pins
certifying NEGATIVES over banked inputs, conditional on the .354/.359
partition, scoped at NRDT's own status -- OPEN, NAMED, NOT ADOPTED. Neither
check re-grades the fence surfaces (tokens untouched -- these checks
certify WHY they stand); neither adopts NRDT; neither claims the dictionary
false or free; neither closes or reopens the .284 exhaustiveness; the
endpoint/absolute-scale side stays with the dimensional-anchor program
(Paper 28). Consequence: no native [P] route to the measured angle exists
under the partition; the fence ceiling is [P_structural_reading] (Paper 44
Sec. 12.7a, the fourth bin) with a certified reason. FALSIFIER: a banked
native structure whose capacity is a function of the index n -- a
Jarlskog-analogue for the correspondence. None exists over the banked
inventory as of the census tree (check 3, v24.3.394); a future banked
constructor consuming the index fails the census sentinel -- the falsifier
is live and now MECHANIZED (an objector must exhibit a constructor the
sentinel will catch).

Dependencies (check 1): L_completion_angle_content_blind_native,
T_ew_load_placement_P, T_theta_QCD, T22.
Dependencies (check 2): T_abelian_coupling_fixed_by_rank1_capacity_count,
T_ew_load_placement_P.
Dependencies (check 3): L_wg2_dictionary_index_content_blind_native,
T_mcross_gate_dictionary_conditional, L_completion_angle_content_blind_native,
T_ew_load_placement_P.
Cross-refs: T_load_form_selected_by_alpha_s, T_sin2theta, T24,
T_gauge_reading_dichotomy, T_ledger_rent_excluded.
Cross-refs (check 3): T_config_demand_register_split_bank_respected,
T_mcross_planck_ratio_composition,
T_abelian_coupling_fixed_by_rank1_capacity_count.
"""

import io
import math
import os
import re
from fractions import Fraction

from apf.apf_utils import check, _result
from apf.abelian_coupling_capacity_count import (
    B2, B3, BY, C_total, SIGMA, d_eff, _alpha_s_forward)
from apf.strong_cp_completion_no_go import (
    check_L_completion_angle_content_blind_native)

_PROBED_FAMILY = (1, 2, 4)   # the probed power family; exhaustiveness its own open


def _native_capacity_at_index(n):
    """Native capacity carried while maintaining dictionary index n.

    The banked native ledger has NO realized-coupling register (the [P]
    ledger chain to 3/13 is coupling-symbol-free; the crossing values enter
    as native counts) -- so no native constructor consumes n, and the
    capacity is C_total = 61 for every n. This is the construction-level
    absence named in the module docstring, read off banked content."""
    check(n in _PROBED_FAMILY, "index outside the probed family")
    return C_total


def _native_invariant_vector(n):
    """The native-preserved data a dictionary completion carries.

    Crucially the index n is ABSENT: every entry is constructed from counts
    and exact rationals with zero coupling symbols, so no entry can be a
    function of n. sigma is the banked intensive quantum SIGMA (imported,
    L_sigma_intensive [P]); the crossing values (C_total/6)*sigma = 47.02
    and C_total = 61 are native counts."""
    check(n in _PROBED_FAMILY, "index outside the probed family")
    x, m = Fraction(1, 2), 3
    a22 = x * x + m
    gamma2 = a22 + 1                     # 17/4: self-competition + radial-Higgs record
    r_star = (x * x + m - gamma2 * x) / (gamma2 - x)
    share = r_star / (1 + r_star)        # 3/13, exact
    return (
        C_total,                          # 61: the rigid capacity count
        d_eff,                            # the interface dimension count
        round(SIGMA, 9),                  # sigma = ln d_eff, imported (banked)
        str(gamma2),                      # '17/4'
        str(share),                       # '3/13'
        (str(Fraction(7)), str(Fraction(19, 6)), str(Fraction(41, 6))),  # |beta| counts
        round((C_total / 6) * SIGMA, 4),  # 47.0206: 1/alpha_cross as a native count
        C_total,                          # 61 again: 1/alpha_Y(M_cross) as a capacity count
    )


def _placement_share(g1, g2):
    """The ledger share for a placement gamma = (g1, g2) (T22 competition
    matrix at x = 1/2, m = 3) -- exact rationals, no coupling symbol."""
    x, m = Fraction(1, 2), 3
    a11, a12, a21, a22 = Fraction(1), x, x, x * x + m
    rs = (g1 * a22 - g2 * a12) / (g2 * a11 - g1 * a21)
    return rs / (1 + rs)


def _own_source():
    fn = os.path.abspath(__file__)
    if fn.endswith(('.pyc', '.pyo')):
        fn = fn[:-1]
    with io.open(fn, 'r', encoding='utf-8') as f:
        return f.read()


def _sentinels():
    """Vocabulary sentinels on this module's own source (audit fix C2).
    The probe tokens are assembled at runtime so the probes cannot self-trip."""
    src = _own_source()
    word = 'vac' + 'uum'
    check(word not in src.lower(),
          "module source must stay free of the fenced sector token (C2)")
    num = str(6 * 7)
    check(re.search(r'(?<![\d.])' + num + r'(?![\d.])', src) is None,
          "module source must carry no standalone occurrence of the fenced count (C2)")
    tok = 'log' + '(' + '102' + ')'
    check(tok not in src,
          "sigma must be IMPORTED (SIGMA, abelian_coupling_capacity_count), "
          "never recomputed from the literal (C2)")


def check_L_wg2_dictionary_index_content_blind_native():
    # --- 0. the partition instrument, called live (audit fix R1) -----------
    anchor = check_L_completion_angle_content_blind_native()
    check(isinstance(anchor, dict), "the .359 anchor must return a result record")
    check(anchor.get('epistemic') == 'P_structural',
          "the .359 partition anchor must hold live at its banked grade "
          "[P_structural] -- this check consumes the partition, not a mirror")

    # --- 1. the index is NO-RECORD: gain(n) = 0 over the probed family -----
    for n in _PROBED_FAMILY:
        check(_native_capacity_at_index(n) == 61,
              "C_total = 61 while maintaining index n=%d -- no native "
              "capacity gain from the index" % n)
    gains = [_native_capacity_at_index(n) - _native_capacity_at_index(2)
             for n in _PROBED_FAMILY]
    check(all(g == 0 for g in gains),
          "gain(n) = 0 for every probed n => the index is NO-RECORD "
          "(the theta-bar side of the .354/.359 partition)")

    # --- 2. the [P] ledger chain, coupling-symbol-free (exact rationals) ---
    x, m = Fraction(1, 2), 3
    a22 = x * x + m
    check(a22 == Fraction(13, 4), "a22 = x^2 + m = 13/4 exactly")
    gamma2 = a22 + 1
    check(gamma2 == Fraction(17, 4), "gamma2 = a22 + n_radial = 17/4 exactly")
    r_star = (x * x + m - gamma2 * x) / (gamma2 - x)
    check(r_star == Fraction(3, 10), "r* = 3/10 exactly")
    share = r_star / (1 + r_star)
    check(share == Fraction(3, 13),
          "the ledger share 3/13 reproduced in exact Fractions -- zero "
          "coupling symbols anywhere in the chain (the load-bearing exclusion, "
          "witnessed by the [P] chain's own arithmetic)")

    # --- 3. the crossing values are native counts (exclusion re-scoped, R3)
    K = (C_total / 6) * SIGMA
    check(abs(K - 47.0206) < 1e-3,
          "1/alpha_cross = (C_total/6)*sigma = 47.02 enters as a native count; "
          "its reading AS a coupling (T20 / L_coupling_capacity_id) is a "
          "distinct banked identification HELD FIXED across the family")
    check(C_total == 61,
          "1/alpha_Y(M_cross) = C_total = 61 enters as the rank-1 capacity "
          "count, index-independent")

    # --- 4. the invariant-vector witness: the index is content-blind -------
    vecs = {n: _native_invariant_vector(n) for n in _PROBED_FAMILY}
    check(vecs[1] == vecs[2] == vecs[4],
          "identical native invariant vector across the probed family "
          "(by construction: no constructor consumes the index)")
    realized_2_exact = 1 / (1 + Fraction(10, 3))
    check(realized_2_exact == Fraction(3, 13),
          "n=2: realized angle = 1/(1 + 10/3) = 3/13 exactly (the ratio "
          "(10/3)^(2/n) at n=2 is 10/3)")
    realized = {n: 1.0 / (1.0 + (10.0 / 3.0) ** (2.0 / n))
                for n in _PROBED_FAMILY}
    check(abs(realized[2] - float(Fraction(3, 13))) < 1e-12,
          "float witness agrees with the exact n=2 value 0.23077")
    check(abs(realized[4] - 0.35389) < 5e-5,
          "n=4: realized angle ~0.35389 off the SAME native vector")
    check(len({round(v, 6) for v in realized.values()}) == 3,
          "identical native data, distinct realized angles => the map "
          "(native data -> index) is not single-valued => CONTENT-BLIND")

    # --- 5. negative control: the SHARE is content-fixed -------------------
    placements = {
        'inert': _placement_share(Fraction(1), Fraction(13, 4)),
        'record-in': _placement_share(Fraction(1), Fraction(17, 4)),
        'two-records': _placement_share(Fraction(1), Fraction(21, 4)),
        'record-on-U1': _placement_share(Fraction(2), Fraction(13, 4)),
    }
    check(set(placements.values()) ==
          {Fraction(13, 35), Fraction(3, 13), Fraction(5, 43), Fraction(13, 19)},
          "the four placement fractions reproduce exactly")
    check(len(set(placements.values())) == 4,
          "four placements, four pairwise-distinct native fractions: the "
          "SHARE is content-FIXED -- the no-go lands exactly on the "
          "seam-crossing index, not as blanket skepticism")

    # --- 6. the scale-placement leg (cut 1): the endpoint is content-blind -
    _, _, t = _alpha_s_forward()
    coeffA, coeffB = BY / (2 * math.pi), B2 / (2 * math.pi)
    endpoint_angles = []
    for dist in (t, t - math.log(2.0), t - math.log(10.0)):
        invY = C_total + coeffA * dist
        inv2 = K - coeffB * dist
        endpoint_angles.append(inv2 / (inv2 + invY))
    check(abs(endpoint_angles[0] - float(Fraction(3, 13))) < 1e-9,
          "at the solve's own endpoint the one-loop angle is exactly the share")
    check(len({round(a, 6) for a in endpoint_angles}) == 3,
          "the realized one-loop angle moves with the endpoint while the "
          "native vector stays fixed: the endpoint placement is content-blind "
          "(cut 1); under cut 2 (instrumented transport) the promotion "
          "arithmetic is the same -- the ceiling is cut-robust")

    # --- 7. vocabulary sentinels (audit fix C2) -----------------------------
    _sentinels()

    return _result(
        name='L_wg2_dictionary_index_content_blind_native -- the w propto g^n '
             'dictionary index is NO-RECORD and content-blind (the fence '
             'ceiling certified)',
        tier=4,
        epistemic='P_structural',
        summary=(
            'Under the .354/.359 record-partition (called live: '
            'check_L_completion_angle_content_blind_native, [P_structural]), '
            'the w propto g^n dictionary index is NO-RECORD (zero native '
            'capacity gain; C_total = 61 for every probed n) and '
            'CONTENT-BLIND: completions at n=2 and n=4 share every '
            'native-preserved invariant (C_total, d_eff, sigma, gamma = '
            '(1, 17/4), share 3/13, |beta| counts, crossing values 47.02 and '
            '61) yet realize distinct angles (3/13 exact vs ~0.35389). The '
            '[P] ledger chain to 3/13 is recomputed coupling-symbol-free in '
            'exact Fractions; the crossing values enter as native counts '
            'whose coupling reading (T20 / L_coupling_capacity_id) is a '
            'distinct banked identification held fixed across the family. '
            'Negative control: the ledger SHARE is content-FIXED (four '
            'placements, four distinct fractions 13/35, 3/13, 5/43, 13/19). '
            'The endpoint placement is content-blind too (cut 1); the ceiling '
            'is cut-robust. Empirically n=2 is the unique survivor of the '
            'probed power family {1,2,4} at the three-output discriminator '
            'grade (artifacts only; family-exhaustiveness its own open). '
            'CONSEQUENCE: no native [P] route to the measured angle exists '
            'under the partition; the fence ceiling is [P_structural_reading] '
            'with a certified reason. The T_sin2theta token is '
            'untouched; NRDT stays OPEN, NAMED, NOT ADOPTED. Falsifier: a '
            'banked native structure whose capacity depends on the index.'
        ),
        key_result=(
            'The w propto g^2 dictionary selecting index is NO-RECORD '
            '(gain(n)=0) and content-blind (identical native invariant vector '
            'across the probed family, distinct realized angles); the ledger '
            'share is content-FIXED (negative control). Fence ceiling '
            '[P_structural_reading] certified with a reason; tokens untouched; '
            'NRDT not adopted.'
        ),
        dependencies=['L_completion_angle_content_blind_native',
                      'T_ew_load_placement_P', 'T_theta_QCD', 'T22'],
        cross_refs=['T_load_form_selected_by_alpha_s', 'T_sin2theta', 'T24',
                    'T_gauge_reading_dichotomy', 'T_ledger_rent_excluded'],
    )


def check_T_mcross_gate_dictionary_conditional():
    # --- 1. the banked forward solve, recomputed live ----------------------
    alpha_s, inv_a2, t = _alpha_s_forward()
    check(abs(t - 34.5923) < 1e-3,
          "t = ln(M_cross/M_Z) = 34.5923: the forward solve DOES supply t "
          "from the counts (the fence's 'which the counts do not supply' was "
          "half-stale as absolute text)")
    check(abs(alpha_s - 0.11790) < 1e-4,
          "alpha_s(M_Z) = 0.11790 reproduced live from the banked solve")

    # --- 2. the index-parameterized solve: t is a function of the index ----
    K = (C_total / 6) * SIGMA
    coeffA, coeffB = BY / (2 * math.pi), B2 / (2 * math.pi)
    t_of, inv_a3_of = {}, {}
    for n in _PROBED_FAMILY:
        ratio = (10.0 / 3.0) ** (2.0 / n)
        t_of[n] = (ratio * K - C_total) / (coeffA + ratio * coeffB)
        inv_a3_of[n] = K - (B3 / (2 * math.pi)) * t_of[n]
    check(abs(t_of[2] - t) < 1e-9,
          "the n=2 member IS the banked forward solve")
    check(abs(t_of[1] - 69.002) < 5e-3,
          "t(1) = 69.00 (the same native counts under w propto g^1)")
    check(abs(t_of[4] - 12.376) < 5e-3,
          "t(4) = 12.38 (the same native counts under w propto g^4)")
    check(len({round(v, 6) for v in t_of.values()}) == 3,
          "t(n) pairwise distinct on IDENTICAL native inputs => t is a "
          "function of the dictionary index => the M_cross gate is "
          "dictionary-index-conditional")
    check(t_of[1] / t_of[4] > 5.0,
          "the supplied t spans a factor > 5 across the probed family "
          "(12.4 - 69.0): the scale seam is NESTED inside the dictionary seam")

    # --- 3. the coupling-free kill (arithmetic-only pass gate, R2) ---------
    check(inv_a3_of[1] < 0,
          "n=1: 1/alpha_3 at the solved endpoint is NEGATIVE -- killed by the "
          "solve's own arithmetic; no measured value consumed in any pass gate")

    # --- 4. seam-token presence (source-level; the token, not the wording) -
    gen_fn = os.path.join(os.path.dirname(os.path.abspath(__file__)),
                          'generations.py')
    with io.open(gen_fn, 'r', encoding='utf-8') as f:
        gen_src = f.read()
    check(gen_src.count("epistemic='P_structural_seam'") >= 4,
          "the P_structural_seam token stands on the fence surfaces "
          "(generations.py: T_sin2theta + the crossing/higgs-record "
          "family; T24 re-scoped to the value claim [P], 2026-07-03 "
          "split) -- this check certifies WHY it stands and survives any "
          "wording pass by probing the token, not the clause")

    # --- 5. vocabulary sentinels (audit fix C2) ----------------------------
    _sentinels()

    return _result(
        name='T_mcross_gate_dictionary_conditional -- the forward solve '
             'supplies t natively but index-conditionally: the scale seam is '
             'nested inside the dictionary seam',
        tier=4,
        epistemic='P_structural',
        summary=(
            'The banked forward solve (_alpha_s_forward, '
            'abelian_coupling_capacity_count.py) supplies t = ln(M_cross/M_Z) '
            '= 34.59 from the counts alone -- so the fence clause "which the '
            'counts do not supply" was half-stale -- BUT the supplied t is '
            'dictionary-index-conditional in its very formulation: the ratio '
            'condition is stated in coupling space, and on identical native '
            'inputs t(n) = 69.0 / 34.6 / 12.4 under w propto g^1/g^2/g^4 '
            '(factor ~5.6). The M_cross scale gate therefore cannot discharge '
            'the scale seam independently of the dictionary; fully closed '
            '(.284 exhaustiveness + the absolute-scale anchor) it still caps '
            'the measured-angle reading at [P_structural_instrument], never '
            'bare [P] -- the measured-angle promotion clause (homed at '
            'T_sin2theta since the 2026-07-03 value/reading split) is '
            'foreclosed as written '
            '(principal-approved reword rides this landing). Pass gates are '
            'arithmetic identities only (t(n) distinctness + the n=1 negative '
            '1/alpha_3 kill); the super-Planckian n=1 row and the n=4 '
            'comparator row live in artifacts at the discriminator grade. '
            'Does not supply t at any grade; does not touch the fence tokens; '
            'the endpoint/absolute-scale side stays with the '
            'dimensional-anchor program (Paper 28).'
        ),
        key_result=(
            't = ln(M_cross/M_Z) IS natively supplied (34.59) but '
            'index-conditional (t(n) spans 12.4 - 69.0 across the probed '
            'family): the M_cross gate is Half-A-conditional, the scale seam '
            'nested inside the dictionary seam; the [P] promotion clause is '
            'foreclosed -- fully closed the gate caps at '
            '[P_structural_instrument].'
        ),
        dependencies=['T_abelian_coupling_fixed_by_rank1_capacity_count',
                      'T_ew_load_placement_P'],
        cross_refs=['T_load_form_selected_by_alpha_s', 'T_sin2theta', 'T24',
                    'T_gauge_reading_dichotomy'],
        artifacts={
            'grade_note': ('empirical rows are ARTIFACTS ONLY, at the '
                           'three-output discriminator grade; never pass-gated'),
            't_by_index': {n: round(t_of[n], 4) for n in _PROBED_FAMILY},
            'n1_row': ('M_cross = %.2e GeV > M_Pl = 1.22e19 GeV (ordering '
                       'inversion of the construction premise; comparison '
                       'anchor M_Z = 91.1876 GeV) AND 1/alpha_3 = %.3f < 0'
                       % (91.1876 * math.exp(t_of[1]), inv_a3_of[1])),
            'n2_row': ('alpha_s(M_Z) = %.5f vs measured 0.1180 (0.11 sigma; '
                       'the banked corroboration)' % alpha_s),
            'n4_row': ('alpha_s(M_Z) = %.4f vs measured 0.1180 (~74 percent '
                       'off; comparator only)' % (1.0 / inv_a3_of[4])),
            'pinning': ('n=2 is the unique survivor of the probed power '
                        'family {1,2,4}; family-exhaustiveness its own open'),
        },
    )


def check_T_wg2_dictionary_index_no_native_consumer_census():
    """The C(n) constructor census: no banked constructor consumes the index.

    CERTIFIED NEGATIVE at bank-closed-world strength, [P_structural] tier 4
    (the .355/.364 census/instrument family), conditional on the .354/.359
    partition called live in-body. Closed-world pin: landed at tree
    v24.3.394, EXPECTED_REGISTRY_SIZE 3880 (the in-check gate is monotone
    ->= only: registry growth alone does not fail the census; the source
    tripwire does). The strength disanalogy with .359 is inherited
    unchanged: this is inventory-certified CONSTRUCTION-LEVEL absence of a
    consumer, not a slot-forbidding lemma. Audit provenance: fresh-context
    walker CERTIFIED NEGATIVE 0.88 + fresh-context hostile audit
    LAND-WITH-FIXES 0.85, all fixes carried (F1 MAJOR: the abelian
    hardwired-index disposition mechanized as the ninth fingerprint;
    F2: Level-T/Level-S regexes pinned deliberately distinct; F3: the
    home-module count-map exclusion guarded by a top-level-callable pin;
    F4: the DX gate stated curation-dependent; F5/F6 notes carried); walk
    of record "The Turning/cn_constructor_census_walk_2026-07-04/".
    Full narrative: the CHECK 3 block of the module docstring."""
    import inspect
    from apf import _module_manifest as mm
    from apf.gauge_quotient_ledger import check_T_ew_load_placement_P
    from apf.strong_cp_completion_no_go import _capacity_gain
    from apf.mcross_planck_ratio_composition import (
        check_T_mcross_planck_ratio_composition)
    from apf.reading_functional_census import CENSUS_ROWS, CLOSURE_MECHANISMS
    from apf.supplements import check_L_QEC_code_space

    # --- 0. the closed world, containment-certified without a registry
    #        import (registry-load safe AND sandbox-gap immune: the census
    #        reads files and the import-safe manifest lists, never imports
    #        a bank module; the scipy-blocked module is still swept) -------
    pkg_dir = os.path.dirname(os.path.abspath(__file__))
    mods = (tuple(mm.BANK_REGISTRY_MODULES)
            + tuple(mm.ARCHITECTURE_ONLY_MODULES)
            + tuple(mm.STANDALONE_LEMMA_MODULES))
    missing = [mp for mp in mods
               if not os.path.isfile(os.path.join(
                   pkg_dir, mp[len('apf.'):].replace('.', os.sep) + '.py'))]
    check(not missing,
          "every manifest module (bank + architecture-only + standalone) "
          "resolves to a file under the package dir -- the Level-S sweep "
          "surface CONTAINS the registered closed world (import-free "
          "containment; missing: %r)" % missing)
    check(mm.EXPECTED_REGISTRY_SIZE >= 3880,
          "closed-world pin: landed at tree v24.3.394 with "
          "EXPECTED_REGISTRY_SIZE 3880; the gate is monotone (>=) so bank "
          "growth alone never fails the census -- forward coverage is the "
          "source tripwire, not this pin")

    # --- 1. the instrument + the .364 wall, called live (R1/F6 pattern) ---
    anchor = check_L_completion_angle_content_blind_native()
    check(anchor.get('epistemic') == 'P_structural',
          "the .354/.359 partition instrument holds live at [P_structural] "
          "-- this census is CONDITIONAL on it (stated, not silently "
          "strengthened)")
    r1 = check_L_wg2_dictionary_index_content_blind_native()
    check(r1.get('epistemic') == 'P_structural',
          "check 1 (index content-blind) holds live at [P_structural]")
    check('NRDT stays OPEN, NAMED, NOT ADOPTED' in r1['summary'],
          "NRDT sentinel: the live check-1 result carries OPEN / NAMED / "
          "NOT ADOPTED verbatim; this census reads the wall, it does not "
          "soften or adopt")
    r2 = check_T_mcross_gate_dictionary_conditional()
    check(r2.get('epistemic') == 'P_structural',
          "check 2 (M_cross gate index-conditional) holds live at "
          "[P_structural]")
    t_by = r2['artifacts']['t_by_index']

    # --- 2. Level V: the native invariant vector under the family ---------
    vecs = {n: _native_invariant_vector(n) for n in _PROBED_FAMILY}
    check(len(set(vecs.values())) == 1,
          "Level V: the native invariant vector, built by the banked "
          "constructor with n live in scope, is IDENTICAL across the "
          "probed family %r" % (_PROBED_FAMILY,))
    check(all(_native_capacity_at_index(n) == 61 for n in _PROBED_FAMILY),
          "Level V: native capacity = 61 at every probed n (gain(n) = 0; "
          "the index is NO-RECORD)")
    check(isinstance(C_total, int) and C_total == 61 and d_eff == 102
          and abs((C_total / 6) * SIGMA - 47.0206) < 1e-3
          and (B3, B2 * 6, BY * 6) == (7.0, 19.0, 41.0),
          "the native-vector inputs are module-level COUNT constants -- no "
          "callable of any coupling exponent anywhere in their construction")

    # --- 3. the realized side, dispositioned by TYPE (+ audit F1 honesty) -
    check(len({round(v, 4) for v in t_by.values()}) == 3
          and t_by[1] / t_by[4] > 5.0,
          "realized-side quantities MOVE with n (t(n) three distinct "
          "values, factor > 5, live from the check-2 artifact): their "
          "n-dependence is the WALL itself, not a candidate C(n) -- t is "
          "a log of a realized scale ratio, typed to coupling space by "
          "check 2")
    _, _, t_fwd = _alpha_s_forward()
    check(abs(t_fwd - t_by[2]) < 5e-4,
          "F1 disposition, carried honestly: _alpha_s_forward IS the n=2 "
          "family member (its ratio hardwires the index; fires ZERO of the "
          "eight vocabulary fingerprints). This disposition was made by "
          "PRIOR LANE KNOWLEDGE (.364), NOT caught by any census level -- "
          "the NINTH fingerprint below carries it mechanized. Its native "
          "outputs are the index-free counts of the Level-V gate; its "
          "realized outputs sit on the far side of the seam")
    r_365 = check_T_mcross_planck_ratio_composition()
    check(r_365.get('epistemic') == 'P_structural_reading'
          and ('dic' + 'tionary-in' + 'dex reading')
              in r_365['artifacts']['rider_ledger'],
          "the .365 composition called live: [P_structural_reading] with "
          "the index reading NAMED as rider (vi) -- realized-side "
          "composition, properly fenced, not a native consumer")

    # --- 4. Level T: the typed (Phi, D, nu) inventory ----------------------
    classes = {r['cls'] for r in CENSUS_ROWS.values()}
    check(len(CENSUS_ROWS) == 23
          and classes == set('G RM EQ RC HR DX CP'.split()),
          "Level T: 23 curated rows across the 7 audited classes")
    dx = [k for k, r in CENSUS_ROWS.items() if r['cls'] == 'DX']
    check(dx == ['L_wg2_dictionary_index_content_blind_native']
          and 'NO-RECORD' in CENSUS_ROWS[dx[0]]['closure']
          and '61' in CENSUS_ROWS[dx[0]]['closure'],
          "Level T: exactly ONE index-typed row exists and it is this "
          "module's fence row (closure = NO-RECORD at capacity 61). "
          "HONEST SCOPE (audit F4): this DX-uniqueness gate is "
          "CURATION-DEPENDENT -- it nets a future consumer only if that "
          "consumer is curated into CENSUS_ROWS; forward coverage is "
          "carried by the Level-S tripwire, and even that is "
          "vocabulary-bounded (see the named residual)")
    # the Level-T instrument: DELIBERATELY DISTINCT from the Level-S
    # fingerprints (audit F2) -- T probes the typed prose fields of a
    # curated inventory (index-consumption vocabulary in functional/
    # support/closure/openness), S probes source-code registers; each is
    # tuned to its surface and unifying them would blunt both.
    idx_tokens = re.compile('dic' + 'tionary[ _-]?in' + 'dex'
                            + r"|g\s*\^\s*n|g\s*\*\*\s*n"
                            + '|coup' + 'ling[- ]exp' + 'onent'
                            + '|w ~ g|w pr' + 'opto g')
    offenders = [k for k, r in CENSUS_ROWS.items() if r['cls'] != 'DX'
                 and idx_tokens.search(' '.join(
                     str(r.get(f, '')) for f in
                     ('functional', 'support', 'closure', 'openness')))]
    check(offenders == ['T_mcross_planck_ratio_composition'],
          "Level T: of the 22 non-DX rows, the ONLY one naming the index "
          "family is the CP composition row -- which names it as an "
          "ADOPTED RIDER (dispositioned live above), not as a consumed "
          "argument")
    check(len(CLOSURE_MECHANISMS) == 3 and not any(
              idx_tokens.search(' '.join(t)) for t in CLOSURE_MECHANISMS),
          "Level T: the three audited closure mechanisms close functional "
          "forks through states and counts -- none consumes a coupling "
          "exponent")

    # --- 5. Level S: the nine-fingerprint bank-closed-world source sweep --
    # probe tokens assembled at runtime (the module C2 pattern) so this
    # check's own source never self-trips its own instruments; the NINTH
    # fingerprint (audit F1) is the hardwired-index ratio shape that the
    # eight vocabulary fingerprints provably miss.
    fps = [
        re.compile('_PRO' + 'BED_FAM' + 'ILY'),
        re.compile(r"\(\s*10(?:\.0)?\s*/\s*3(?:\.0)?\s*\)\s*\*\*"),
        re.compile('10/3' + r"\)\s*\^\s*\(2/n\)"),
        re.compile(r"\bg\s*\*\*\s*" + 'n' + r"\b"),
        re.compile('g' + r"\^" + 'n'),
        re.compile('w pr' + 'opto g'),
        re.compile('w-pr' + 'opto-g'),
        re.compile('dic' + 'tionary[ _-]in' + 'dex'),
        re.compile(r"\(\s*1\s*-\s*" + 'SIN2_TH' + r"ETA_W\s*\)\s*/\s*"
                   + 'SIN2_TH' + 'ETA_W'),
    ]

    def _sweep(text):
        return sum(len(rx.findall(text)) for rx in fps)

    hits = {}
    for root, dirs, files in os.walk(pkg_dir):
        dirs[:] = [d for d in dirs if d != '__pycache__']
        for fname in sorted(files):
            if not fname.endswith('.py'):
                continue
            p = os.path.join(root, fname)
            with io.open(p, encoding='utf-8', errors='replace') as fh:
                n_hits = _sweep(fh.read())
            if n_hits:
                hits[os.path.relpath(p, pkg_dir).replace(os.sep, '/')] = n_hits

    # the home module: EXCLUDED from the count map (its own executable
    # index use is diagnostic and adding this check's source to its own
    # pinned file would be self-hit unstable) -- and guarded STRUCTURALLY
    # instead (audit F3): the set of top-level callables is pinned, so a
    # future constructor added inside this module fails until
    # re-dispositioned.
    home_hits = hits.pop('wg2_dictionary_typing_no_go.py', 0)
    check(home_hits > 0,
          "the home module fires the fingerprints (it is the fence; "
          "executable index use is diagnostic) -- excluded from the count "
          "map, guarded by the callable pin below")
    top_defs = set(re.findall(r'^def (\w+)', _own_source(), re.M))
    check(top_defs == {
              '_native_capacity_at_index', '_native_invariant_vector',
              '_placement_share', '_own_source', '_sentinels',
              'check_L_wg2_dictionary_index_content_blind_native',
              'check_T_mcross_gate_dictionary_conditional',
              'check_T_wg2_dictionary_index_no_native_consumer_census',
              'register', 'run_all'},
          "F3 structural gate: the home module's top-level callables are "
          "pinned -- a new constructor added inside the excluded file "
          "fails here until re-dispositioned")

    EXPECTED_HITS = {
        'generations.py': (30,
            'seam-family fence PROSE (T_sin2theta family); seam tokens '
            'gated below as standing'),
        'mcross_planck_ratio_composition.py': (11,
            'rider-(vi) prose; the composition dispositioned LIVE above at '
            'its banked reading grade'),
        '_module_manifest.py': (14,
            'infra changelog prose, including this landing''s own '
            'narrative line; not a registered surface'),
        'reading_functional_census.py': (5,
            'the DX census row''s own prose; non-registered analysis '
            'layer; DX-uniqueness gated above'),
        'gauge_quotient_ledger.py': (3,
            'fence prose inside the [P] chain; the chain consumed live '
            'below, coupling-symbol-free'),
        'codomain_transport_schema.py': (3,
            'the dictionary-fence transport schema strings; the pre-.364 '
            'ruling this census strengthens'),
        'ie_wall_shadow_census.py': (2,
            'v24.3.401 wall-shadow census: the two hits are the W1 '
            'certificate NAMES pinned in its WALL_REGISTRY (that census '
            'RUNS this fence as a wall certificate; it consumes no '
            'index); its prose is index-symbol-free by construction'),
        'abelian_coupling_capacity_count.py': (1,
            'F1, THE ONE KNOWN IMPLICIT MEMBER: the hardwired n=2 ratio '
            '(1-s)/s -- fires ZERO of the eight vocabulary fingerprints; '
            'dispositioned by PRIOR LANE KNOWLEDGE (.364), not by a census '
            'level; the ninth fingerprint carries the disposition '
            'mechanized. Native outputs: index-free counts (Level-V gate); '
            'realized outputs: far side of the seam (typed by check 2)'),
    }
    check(set(hits) == set(EXPECTED_HITS),
          "Level S: the package-recursive nine-fingerprint sweep returns "
          "EXACTLY the dispositioned files -- no undispositioned surface "
          "anywhere in the package touches the index (found: %r)"
          % sorted(hits))
    check(all(hits[k] == EXPECTED_HITS[k][0] for k in hits),
          "Level S: per-file hit counts match the pin exactly (%r) -- the "
          "count-pin tripwire is armed: count drift on a dispositioned "
          "file, or a new firing file, fails until re-dispositioned"
          % {k: v for k, v in sorted(hits.items())})

    # --- 6. negative controls (all mandatory) ------------------------------
    r_p = check_T_ew_load_placement_P()
    check(r_p.get('epistemic') == 'P'
          and r_p['artifacts']['ledger_fraction'] == '3/13',
          "the [P] ledger chain called live: share 3/13 at [P] -- read, "
          "not re-graded")
    fracs = ({Fraction(3, 13)}
             | {Fraction(v) for v in
                r_p['artifacts']['rivals_that_miss'].values()})
    check(fracs == {Fraction(3, 13), Fraction(13, 35), Fraction(5, 43),
                    Fraction(13, 19)} and len(fracs) == 4,
          "control (a): four placements, four pairwise-distinct native "
          "fractions (live from the [P] chain artifact) -- native CONTENT "
          "moves the constructed share; the INDEX moves nothing native")

    SYNTH_SRC = ('def synthetic_Cn(n):\n'
                 '    # SEEDED CONSUMER: capacity reads the register g ** n\n'
                 '    return 61 + (n - 2)\n')

    def synthetic_Cn(n):
        # F6 note: at n=2 the seed EQUALS the true capacity by construction
        # of the zero offset -- a coincidence of the seed design, not a use
        # of the empirical pin (which enters no pass gate in this check).
        return 61 + (n - 2)

    synth = {n: synthetic_Cn(n) for n in _PROBED_FAMILY}
    check(len(set(synth.values())) == 3,
          "control (b), value level: the seeded consumer is NON-constant "
          "over the probed family -- the gate the real inventory passes "
          "FAILS on the seed: caught")
    check(_sweep(SYNTH_SRC) >= 1,
          "control (b), source level: the seeded consumer's source fires "
          "the fingerprint -- the scan is falsifiable, not vacuous")
    check(len(set(vecs.values())) == 1 and len(set(synth.values())) > 1,
          "control (b), joint: identical predicates, opposite verdicts on "
          "inventory vs seed -- the census discriminates")
    check(_capacity_gain('delta_CKM') == 6 > 0 == _capacity_gain('theta_bar'),
          "control (c1): under the SAME record partition, delta_CKM is "
          "RECORD-BEARING (capacity gain 6, live from the .359 instrument) "
          "while theta-bar and the index gain zero -- positives where "
          "positives exist")
    S_code = check_L_QEC_code_space()['artifacts']['S_code']
    check(abs(S_code - C_total * SIGMA) < 1e-3,
          "control (c2) anchor: S_code = C_total * sigma consumed live "
          "from the banked QEC surface")
    check(len({round(K * SIGMA, 6) for K in (60, 61, 62)}) == 3,
          "control (c2): a genuine COUNT-exponent (the tensor power "
          "C_total in d_eff^C_total) IS consumed natively -- S(K) "
          "non-constant in K -- the predicate distinguishes "
          "count-exponents (CONSUMED) from the coupling index (not)")

    # --- 7. discipline sentinels -------------------------------------------
    own_fn = inspect.getsource(
        check_T_wg2_dictionary_index_no_native_consumer_census)
    measured = ('0.23' + '122', '0.11' + '80', '91.' + '1876',
                '0.11' + ' sigma')
    check(all(tok not in own_fn for tok in measured),
          "the empirical n=2 pin's measured values appear NOWHERE in this "
          "check's source -- artifacts only, never a pass gate (R2 "
          "discipline; tokens runtime-assembled so the sentinel cannot "
          "self-trip)")
    with io.open(os.path.join(pkg_dir, 'generations.py'), 'r',
                 encoding='utf-8') as f:
        gen_src = f.read()
    check(gen_src.count("epistemic='P_structural_seam'") >= 4
          and r_p.get('epistemic') == 'P',
          "token sentinel: the seam tokens stand untouched on the fence "
          "surfaces and the [P]-chain grade is unmoved -- the census "
          "re-grades nothing")
    _sentinels()

    return _result(
        name='T_wg2_dictionary_index_no_native_consumer_census -- the C(n) '
             'constructor census: no banked constructor consumes the '
             'dictionary index (CERTIFIED NEGATIVE, bank-closed-world, '
             'tripwired forward)',
        tier=4,
        epistemic='P_structural',
        summary=(
            'Over the FULL bank registry at the census tree (closed-world '
            'pin: landed at v24.3.394, EXPECTED 3880; containment certified '
            'import-free against the manifest module lists), NO banked '
            'constructor consumes the dictionary index n in the w-to-g '
            'correspondence. Three coordinated levels: Level V -- the '
            'native invariant vector, built by the banked constructor with '
            'n live in scope, is identical over the probed family and the '
            'native capacity is 61 at every n; Level T -- the audited '
            '(Phi, D, nu) reading-functional inventory carries exactly ONE '
            'index-typed row and it is this module fence row (the '
            'DX-uniqueness gate, honestly curation-dependent per audit F4); '
            'Level S -- a package-recursive NINE-fingerprint source sweep '
            'returns exactly the pinned disposition map of fence and '
            'fenced-reading surfaces (home module excluded from the map, '
            'guarded by a top-level-callable pin per audit F3). Audit F1 '
            'carried in-code: the one known implicit member (the hardwired '
            'n=2 ratio in abelian_coupling_capacity_count) fires zero '
            'vocabulary fingerprints and was dispositioned by prior lane '
            'knowledge, not by a census level -- the ninth fingerprint now '
            'carries that disposition mechanized, and the named residual is '
            'broadened to the demonstrated hardwired-index shape (a '
            'constructor riding a fixed index presents as a constant, '
            'invisible to all three levels) plus vocabulary-avoiding '
            'consumers and non-.py payloads (F5). Negative controls: the '
            'ledger share is content-FIXED (four placements, four distinct '
            'fractions, live); a seeded synthetic consumer is caught at '
            'both the value and the source level (F6 coincidence note '
            'in-code); delta_CKM (capacity gain 6) and the count-exponent '
            'C_total in d_eff^C_total return CONSUMED under the same '
            'predicates. CONSEQUENCE: the module falsifier upgrades from '
            '"none is visible" to "none exists over the banked inventory '
            'as of the census tree, tripwired forward" -- and stays LIVE: '
            'a future banked constructor consuming the index fails this '
            'census sentinel. The .359 disanalogy is inherited unchanged '
            '(construction-level census, NOT slot-impossibility); '
            'conditional on the .354/.359 partition (called live); NRDT '
            'stays OPEN, NAMED, NOT ADOPTED; the empirical n=2 rows stay '
            'artifacts-only; family-exhaustiveness of the probed set and '
            'the .284 exhaustiveness are untouched; no token re-graded; '
            'the ew: atlas verdict block is unmoved.'
        ),
        key_result=(
            'CERTIFIED NEGATIVE: no banked constructor consumes the '
            'dictionary index -- certified at bank-closed-world strength '
            'over Levels V (value), T (typed) and S (source, nine '
            'fingerprints incl. the F1 hardwired-index shape), with the '
            'seeded-consumer and discrimination controls passing in both '
            'directions; the .364 falsifier is now live AND mechanized; '
            'construction-level absence, not slot-impossibility.'
        ),
        dependencies=['L_wg2_dictionary_index_content_blind_native',
                      'T_mcross_gate_dictionary_conditional',
                      'L_completion_angle_content_blind_native',
                      'T_ew_load_placement_P'],
        cross_refs=['T_config_demand_register_split_bank_respected',
                    'T_mcross_planck_ratio_composition', 'T_sin2theta',
                    'T24',
                    'T_abelian_coupling_fixed_by_rank1_capacity_count'],
        artifacts={
            'closed_world_pin': (
                'landed at tree v24.3.394, EXPECTED_REGISTRY_SIZE 3880; '
                'live manifest value at this run: %d (monotone gate only)'
                % mm.EXPECTED_REGISTRY_SIZE),
            'disposition_map': {k: {'hits': v[0], 'disposition': v[1]}
                                for k, v in EXPECTED_HITS.items()},
            'home_module_hits_diagnostic': home_hits,
            'named_residual': (
                'BROADENED per audit F1(ii): (1) the hardwired-index shape '
                '-- a banked structure riding a FIXED n presents as a '
                'constant and is invisible to all three levels; the '
                'abelian member is the live demonstration, now carried by '
                'the ninth fingerprint; (2) vocabulary-avoiding consumers '
                '(self-announcing surfaces only; dispositions are recorded '
                'human judgment); (3) non-.py data payloads are unswept '
                '(F5); (4) the DX gate is curation-dependent (F4)'),
            'falsifier': (
                'a banked native structure whose capacity is a function of '
                'the index n. None exists over the banked inventory as of '
                'the census tree; a future banked constructor consuming '
                'the index FAILS this census sentinel -- live and '
                'forward-tripwired'),
            'audit_provenance': (
                'fresh-context walker CERTIFIED NEGATIVE 0.88 + '
                'fresh-context hostile audit LAND-WITH-FIXES 0.85; walk of '
                'record: The Turning/cn_constructor_census_walk_2026-07-04/'),
        },
    )


_CHECKS = {
    'L_wg2_dictionary_index_content_blind_native':
        check_L_wg2_dictionary_index_content_blind_native,
    'T_mcross_gate_dictionary_conditional':
        check_T_mcross_gate_dictionary_conditional,
    'T_wg2_dictionary_index_no_native_consumer_census':
        check_T_wg2_dictionary_index_no_native_consumer_census,
}


def register(registry):
    """Register the dictionary typing no-go into the bank."""
    registry.update(_CHECKS)


def run_all():
    return {name: fn() for name, fn in _CHECKS.items()}


if __name__ == '__main__':
    for _n, _r in run_all().items():
        print(('PASS' if _r.get('passed', True) else 'FAIL'), _n)
        print('  grade:', _r['epistemic'], '| tier', _r['tier'])
        print('  ', _r['key_result'])


# ---------------------------------------------------------------------------
# Interface Engine onboarding (Full Bank Onboarding wave, 2026-07-04)
# ---------------------------------------------------------------------------

IE_DECLARATIONS = (
    {
        "input_id": "ew:wg2_dictionary_typing_no_go",
        "axis": "ROUTE",
        "route": "wg2_dictionary",
        "expect_export": False,
        "payload": {
            "name": "wg2_dictionary_typing_no_go",
            "closure_kind": "obstruction_named",
            "obstruction_class": "WG2_DICTIONARY_INDEX_NO_RECORD",
            "knockout_summary": (
                "The w-propto-g^n dictionary index is CONTENT-BLIND and "
                "types NO-RECORD: maintaining any probed index n yields "
                "zero native capacity gain (C_total = 61 for every n), so "
                "the fence holding T_sin2theta and its seam family at "
                "[P_structural_seam] is a THEOREM-BACKED CEILING, not a "
                "ruling. ONE WALL; the M_cross gate is index-conditional "
                "and the promotion clause is FORECLOSED for bare [P]; "
                "falsifier live. The ledger share sin^2 theta_W = 3/13 "
                "itself stays [P] (T_ew_load_placement_P) -- the wall is "
                "the dictionary crossing, not the share. "
                "(check_L_wg2_dictionary_index_content_blind_native + "
                "check_T_mcross_gate_dictionary_conditional, "
                "wg2_dictionary_typing_no_go.py)"
            ),
        },
        "note": (
            "Onboards the .364 dictionary fence onto the ROUTE axis as a "
            "closure-by-design obstruction. A future lane reading the "
            "measured weak mixing angle through the dictionary at bare [P] "
            "fails the pin."
        ),
    },
)
