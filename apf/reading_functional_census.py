"""The reading-functional census -- the 2026-07-03 reading-exhaustiveness
walk's analysis layer, with the hostile audit's corrections carried.

NOT a bank module (no register()): analysis/curation layer beside
apf/crystal_ledger.py (the .353 pattern). The walk's one registered surface
is the narrow sentinel check_L_abelian_support_openness_pins
(apf/acc_reading_selection.py, v24.3.369); everything here is reference
content for humans and future walkers, deliberately NOT bank-registered.

THE SCHEMA -- ORGANIZATIONAL, not new mathematics (audit correction). A
reading of the capacity ledger is curated as a triple R = (Phi, D, nu): the
reading FUNCTIONAL Phi (the mathematical species through which a structure
consumes the ledger scalars), the SUPPORT D (which of the 61 slots at
d_eff = 102 the functional is evaluated over), and the semantic clause nu
(what the output is read as). The triple is a filing system for banked
instances; it forces nothing and proves nothing by itself.

  (C1) SUPPORT-FORCING FOLLOWS FUNCTIONAL TYPE (census regularity, no
       banked counterexample). Wherever the bank forces a support, the
       forcing factors through a restriction supplied by Phi's own species:
       the rank-2 Fisher fixed-point equilibrium forces D = B = C_total/6
       running modes (L_coupling_capacity_id [P]); the Gaussian
       root-determinant forces D = the 16 bosonic-quadratic slots (Grassmann
       slots give det^(+1), no root-volume -- the .184 measure-type
       exclusion); the equipartition functional on the saturation state
       forces the 42/19 residual partition (L_equip [P]); the set-inclusion
       classifier exhausts {Sep, IJC} by logic (T_IJC_dichotomy). Where the
       functional is TYPE-BLIND -- the rank-1 abelian's bare additive trace
       -- no restriction exists and the support is an unforced input.
       CORRECT SCOPE WORDING (audit F2; the refuted "trace-blindness kills
       support-forcing" entailment is NOT carried here): the reading VALUE
       carries no support information; the banked structure list supplies
       no selector (the proved sub-fact [P],
       L_abelian_no_ledger_channel_structure); the structure-list
       exhaustiveness stays OPEN (.284).

  (C2) THE FUNCTIONAL-IDENTIFICATION STEP is base-forced only through
       THREE banked closure mechanisms (honest enumeration; audit
       correction -- not "one state-canonical case"):
         1. STATE-CANONICAL: L_equip -- the functional is forced because
            the saturation state is the UNIQUE argmax-entropy state, i.e.
            the trace state (cite BOTH L_equip and L_KMS_trace_state).
         2. FIXED-POINT: L_coupling_capacity_id -- Phi base-forced via the
            rank-2 Fisher fixed point.
         3. INVARIANCE: T_ew_sqrtNc_carrier_forced_by_color_triplet_trace
            -- the functional fork closed by basis-change invariance.
       Everywhere else the identification is proven non-base-derivable
       (the .314 countermodel family, EW form), relocated-not-derived
       (the .284 record, abelian), adopted-with-falsifier
       (UB_usage_billing_adopted), an adjudicated empirical fork
       (T_vacuum_o1_reading_fork), content-blind/fenced (the .364
       dictionary index), or constitutive vocabulary (the IJC
       operationalization). For gauge readings, state-canonical closure is
       UNAVAILABLE by the not-a-state-function corollary (recomputed live
       in the registered sentinel): the abelian 61 and non-abelian 47.02
       read the SAME S_dS = 282.12 and return different values.

THE ASYMMETRY, PRINCIPLED -- MODULO THE PHI-IDENTIFICATION (.314 terminal;
audit correction). Why is bosonic-16 admissible-and-forced where charged-45
is inadmissible-only-by-adoption? Because the floor's functional (a
root-determinant) is TYPED -- its species IS the support selector -- while
the abelian's functional (a bare trace) is type-blind and supplies none.
The 16 is forced CONDITIONAL on the reservoir reading (which root-measure
functional the floor IS -- the identification step, .314 terminal); the
abelian residual is the missing typed functional (.284 open). The two
residuals are DIFFERENT SLOTS of one schema, not a tension.

HONEST NON-CLAIMS.
  - This census CLOSES NOTHING: no support-uniqueness, no exhaustiveness,
    no grade moves. It records a regularity over banked instances, not a
    theorem that the pattern exhausts possible readings -- that would be
    the same shape of openness one level up, and is not claimed.
  - The five dead .284 routes stay dead; occupancy/QAC, the wg2
    family-exhaustiveness, the a = b identity, and the UB adoption are
    each their own settled or named-open kernel; none is touched.
  - The walk's scan-hygiene material (the 21-failure decomposition) is
    deliberately NOT census content and is not carried here.

FALSIFIER HOOKS (live in the registered sentinel, not here): the .284
flag pins, the not-a-state-function recomputation, the two-pronged 45-kill
recomputation -- see check_L_abelian_support_openness_pins.
"""
from __future__ import annotations

# Curated instance table. Support notation: (count, of which ledger,
# selector). Classes: G gauge couplings referred to S_dS at the crossing;
# RM root-measures (sub-support by capacity TYPE); EQ equipartition
# residual partitions; RC regime/branch classifiers; HR horizon /
# holographic readings of a [P] identity; DX dictionary/index readings
# (adjacent, own named open); CP compositions of adopted readings.
CENSUS_ROWS = {
    # -- class G: gauge couplings referred to S_dS at the crossing --------
    "T_abelian_coupling_fixed_by_rank1_capacity_count": dict(
        cls="G", module="abelian_coupling_capacity_count",
        functional="bare additive trace (type-blind)",
        support="61 of 61 (full) -- ADOPTED",
        closure="NONE for the support (5 dead .284 routes); "
                "alpha_s-corroborated (0.11 sigma)",
        openness="structure-list exhaustiveness OPEN (.284)"),
    "T_gauge_reading_dichotomy": dict(
        cls="G", module="acc_reading_selection",
        functional="trace vs fixed-point pair, claimed exhaustive",
        support="{B = 61/6 running, 61 full}",
        closure="rank mechanism [P]; support NOT forced",
        openness="no-third-reading OPEN (Export flag = 0, pinned live)"),
    "T_rank_field_selector": dict(
        cls="G", module="acc_reading_selection",
        functional="scalar selector K vs S_dS", support="rank-selected",
        closure="mechanism [P]", openness="inherits the dichotomy OPEN"),
    "L_abelian_no_ledger_channel_structure": dict(
        cls="G", module="acc_reading_selection",
        functional="structure-list scan", support="sub-fact only",
        closure="sub-fact [P]: group + beta miss the ledger; supplies no "
                "selector (F2 wording)",
        openness="structure-list exhaustiveness OPEN"),
    "T_gauge_value_chain_is_P": dict(
        cls="G", module="acc_reading_selection",
        functional="composition over both arms", support="value chain",
        closure="crossing arm [P] composition",
        openness="abelian arm inherits OPEN"),
    "L_coupling_capacity_id": dict(  # non-abelian arm, for the asymmetry row
        cls="G", module="supplements",
        functional="rank-2 Fisher fixed-point equilibrium (TYPED)",
        support="B = C_total/6 running modes",
        closure="FORCED: closure mechanism 2 (fixed point) [P]",
        openness="n/a (forced, not chosen)"),
    # -- class RM: root-measure / capacity-TYPE sub-support ---------------
    "T_ew_bosonic_enforcement_reservoir_theorem": dict(
        cls="RM", module="ew_bosonic_enforcement_reservoir",
        functional="Gaussian root-determinant (TYPED)",
        support="16 of 61 (bosonic slots)",
        closure="COUNT forced-by-exclusion (.184: Grassmann det^(+1), no "
                "root-volume); conditional on the Phi-identification",
        openness="residual = the FORM (.314 no-go), not the count"),
    "T_ew_pre_branch_necessity": dict(
        cls="RM", module="ew_pre_branch_necessity",
        functional="the exclusion theorem itself (de-circularized)",
        support="16 of 61",
        closure="forcing theorem for the count",
        openness="framing modulo the A/B identifications"),
    "T_ew_scale_functional_independence_no_go": dict(
        cls="RM", module="ew_scale_functional_independence",
        functional="the FORM slot itself",
        support="n/a (form, not support)",
        closure="NO-GO: form not base-derivable (countermodel family, .314 "
                "TERMINAL) -- the Phi-identification residual is named here",
        openness="terminal; >= 2 completions closed-world"),
    "T_ew_static_well_factorization_principle": dict(
        cls="RM", module="ew_static_well_factorization",
        functional="static-well factorization reading of the floor",
        support="rides the 16-slot reservoir",
        closure="reading grade [P_structural_reading]",
        openness="rides the .314 Phi-identification residual"),
    "T_ew_sqrtNc_carrier_forced_by_color_triplet_trace": dict(
        cls="RM", module="ew_sqrtNc_carrier_color_triplet",
        functional="sqrt(N_c) carrier normalization",
        support="colour-triplet trace",
        closure="closure mechanism 3 (INVARIANCE): the functional fork "
                "closed by basis-change invariance",
        openness="reading grade on the surrounding chain"),
    # -- class EQ: equipartition residual partitions ----------------------
    "L_equip": dict(
        cls="EQ", module="cosmology",
        functional="equipartition on the saturation state (TYPED, "
                   "state-canonical)",
        support="61 of 61 uniform; residuals 42/19",
        closure="FORCED: closure mechanism 1 (state-canonical) [P] -- the "
                "saturation state is the unique argmax-entropy state, the "
                "trace state (L_equip + L_KMS_trace_state)",
        openness="n/a (forced)"),
    # -- class RC: regime/branch classifiers ------------------------------
    "T_IJC_dichotomy": dict(
        cls="RC", module="core",
        functional="set-inclusion classifier on the FD5 vocabulary",
        support="{Sep, IJC} branches",
        closure="dichotomy exhaustive BY LOGIC on the vocabulary; no "
                "within-vocabulary gap",
        openness="vocabulary robustness CLOSED (L_graded_threat_collapses_to_"
                 "crisp, v24.3.408 [P_structural]): graded threat structures "
                 "collapse to a threshold-stack of crisp Sep/IJC; third branch "
                 "<=> graded-FD5 violation (excluded); fence = commensurable "
                 "grading (well-typing precondition)"),
    "T_no_IJC_no_noncommutativity": dict(
        cls="RC", module="core",
        functional="branch-(Sep) countermodel",
        support="same vocabulary",
        closure="countermodel EXISTENCE [computed]",
        openness="vocabulary-robustness residual CLOSED with the dichotomy "
                 "(L_graded_threat_collapses_to_crisp, v24.3.408)"),
    # -- class HR: horizon/holographic readings of a [P] identity ---------
    "L_RT_capacity": dict(
        cls="HR", module="supplements",
        functional="marginal-count identity",
        support="k of 61 marginal",
        closure="identity [P]; the ENTANGLEMENT reading NOT forced",
        openness="two-sided reading EXCLUDED (L_RT_two_sided_reading_no_go "
                 "[P]); reading split pinned"),
    "T_vacuum_o1_reading_fork": dict(
        cls="HR", module="vacuum_o1_fork",
        functional="O(1) horizon-coefficient reading",
        support="{3/8, 42/102} two-branch fork",
        closure="NONE (adjudicated empirical fork; H0 discriminator)",
        openness="fork preserved by charter; neither branch forced"),
    # -- class DX: dictionary/index readings (adjacent, own named open) ---
    "L_wg2_dictionary_index_content_blind_native": dict(
        cls="DX", module="wg2_dictionary_typing_no_go",
        functional="dictionary index n in the w ~ g^n family",
        support="index space, not ledger slots",
        closure="NO-RECORD: index content-blind (native capacity 61 at "
                "every probed n; .364)",
        openness="ITS OWN named open (family-scoped); NOT this walk's"),
    # -- class CP: compositions of adopted readings -----------------------
    "T_mcross_planck_ratio_composition": dict(
        cls="CP", module="mcross_planck_ratio_composition",
        functional="P3-composition of adopted readings",
        support="composes the abelian 61, the floor 16, the crossing B",
        closure="composition [P_structural_reading] with SIX adopted "
                "readings named severally (riders i-vi, .365): the .284 "
                "rank-1 support, the floor reading/seam chain + .314, "
                "tree-level M_Z, leading-log order, the route-b anchor, "
                "the .364 dictionary index",
        openness="each rider is its own named open; none silently closed"),
    # -- adjacent named-reading families (filed, not re-adjudicated) ------
    "T_master_equation_form": dict(
        cls="HR", module="recruitment",
        functional="master-equation rate-form reading",
        support="rate slot",
        closure="form reading at its own banked grade",
        openness="own token; filed for completeness"),
    "photon_masslessness_family": dict(
        cls="RC", module="photon_masslessness",
        functional="record-state axis readings (8 checks: masslessness / "
                   "radiation arrow / force character / charge "
                   "quantization / gauge-group enumeration / gravity axis "
                   "/ locked-record mass / colour record)",
        support="record-state axis, not ledger slots",
        closure="each at its own banked token",
        openness="own tokens; filed for completeness"),
    "yang_mills_md_bridge_trio": dict(
        cls="RC", module="yang_mills_md_bridge",
        functional="MD-bridge readings (gap positivity from MD / "
                   "singlet-gapped spectrum typing / the .368 IR endpoint "
                   "trichotomy with readings R1(i)-(iii) NAMED)",
        support="matter-free IR endpoint",
        closure="branch 3 excluded by bank; branch 2 OPEN in the check "
                "name (.368)",
        openness="the inert-plateau branch is its own staged open"),
    "cmb_finite_mode_sextet": dict(
        cls="EQ", module="cmb_finite_mode_covariance",
        functional="finite-mode covariance readings (6 checks; "
                   "scipy-gated in-sandbox, Bucket A)",
        support="low-multipole mode budget",
        closure="finite-mode reduction at its own token",
        openness="likelihood-level test is the named open (Paper 19 arena)"),
    # -- the top rule itself ----------------------------------------------
    "T_acc_reading_selection": dict(
        cls="G", module="acc_reading_selection",
        functional="the composed rule-table (rank + type)",
        support="all four instances",
        closure="composition; open instances named in-check",
        # RULED 2026-07-03 (principal): [P_structural] -- the field was conformed
        # to the docstring headline the same day (v24.3.370); the registered
        # sentinel's grade pin re-pinned in the same pass.
        openness="token ruled [P_structural] 2026-07-03; open instances: EW-floor type clause + abelian sub-support"),
}

CLOSURE_MECHANISMS = (
    ("state-canonical", "L_equip + L_KMS_trace_state",
     "argmax-entropy uniqueness == the trace state"),
    ("fixed-point", "L_coupling_capacity_id",
     "Phi base-forced via the rank-2 Fisher fixed point"),
    ("invariance", "T_ew_sqrtNc_carrier_forced_by_color_triplet_trace",
     "functional fork closed by basis-change invariance"),
)


def census(cls: str | None = None) -> dict:
    """Return the curated rows (optionally filtered by class)."""
    if cls is None:
        return dict(CENSUS_ROWS)
    return {k: v for k, v in CENSUS_ROWS.items() if v["cls"] == cls}
