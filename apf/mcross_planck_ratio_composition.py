"""The M_cross collapse: M_cross/M_Pl composes to a pure capacity number
[P_structural_reading].

Composes the "lone remaining un-collapsed ratio" surface -- a surface stale
in its GRADE LABELS and its sin^2theta_W-gate identification; the "open
content = v_H/M_Pl" pin is a deliberate fence that STANDS: the .314 no-go
proves the form v_H/M_Pl is not base-derivable, so this composition
repackages the hierarchy at reading grade, it does not close it. The
single-anchor scale-web (planck_magnitude_single_anchor, v24.3.187/.225)
named M_cross/M_Pl as the one un-collapsed ratio; the same module banks
v_H/M_Pl as a pure capacity number, and the abelian rank-1 count (v24.3.215,
regraded .284) supplies t = ln(M_cross/mu*) with zero measured couplings.
This module composes the two, so the ratio is a forward-computed pure
capacity number end to end:

    M_cross/M_Pl = e^t * (M_Z/v_H) * (v_H/M_Pl)

with every factor banked:
  e^t        -- the zero-input forward solve, recomputed here independently
                from {C_total = 61, sigma = ln 102, sin^2 theta_W = 3/13,
                |b_2| = 19/6, |b_Y| = 41/6}. Exact closed form:
                t = (4 pi / 313) * (305 ln 102 - 549) = 34.592316.
                The endpoint mu* is DEFINED by the angle condition
                sin^2 theta_W(mu*) = 3/13 -- the scale the bank identifies
                with M_Z (T_sin2theta).
  M_Z/v_H    -- the tree-level EW mass relation M_Z^2 = (g^2 + g'^2) v_H^2 / 4
                evaluated at the solve's own endpoint couplings. Because the
                endpoint satisfies 1/alpha_Y = (10/3)(1/alpha_2) exactly, this
                collapses to the closed form
                M_Z/v_H = sqrt(13 pi / (10 * (1/alpha_2(mu*)))) = 0.371536,
                i.e. a tree M_Z of 91.478 GeV (physical 91.1876: the +0.32%
                residual is the EW radiative-correction layer the bank fences
                as OPEN -- ew_sector_closure.py holds the physical on-shell
                M_Z as external_input). SM non-GUT hypercharge normalization
                throughout (alpha_Y with b_Y = 41/6; NOT the GUT-norm
                alpha_1 = (5/3) alpha_Y, b_1 = 41/10).
  v_H/M_Pl   -- the banked EW floor, sqrt(N_c) (4 pi)^-1 102^-8 (12/7)
                (planck_magnitude_single_anchor).

RESULT:  M_cross/M_Pl = 7.9047e-3, M_cross = 9.651e16 GeV (form a, pure
capacity, tree-level M_Z), against the comparator form (b) M_cross =
e^t * M_Z(measured) = 9.620e16 GeV. The two differ by exactly the tree-vs-
physical M_Z gap (+0.32%). Neither rides Route 1 (supplements.py
L_epsilon_star_Planck Part II, which consumes 1/alpha_EM = 128.2075): the
composition consumes NO measured coupling anywhere. The endpoint's own
1/alpha_em = 1/alpha_2 + 1/alpha_Y = 128.2075 reproduces L_alpha_em's derived
value as an internal cross-formula identity -- by construction of the shared
one-loop system, content-equivalent to the alpha_s gate, NOT independent
corroboration; the honest world comparator is PDG-2024 127.930 (0.22 percent).

IDENTIFICATION. The identification mu* = tree M_Z is definitional at tree
level; its corroboration is the +0.32 percent residual against the measured
M_Z (carried by the gap gate). And the ratio is invariant under
M_Pl -> lambda * M_Pl (it is a ratio), so nothing here touches the
single-anchor no-go: converting M_cross/M_Pl into GeV still consumes the one
route-b dimensional anchor, exactly as every other mass in the scale-web does.

GRADE -- [P_structural_reading], the weakest link, with the rider ledger
named explicitly:
  (i)   the rank-1 support reading 1/alpha_Y(M_cross) = 61 (not the charged
        subset 45) is the .284 adopted reading, alpha_s-corroborated at
        0.11 sigma; its exhaustiveness is OPEN and its five derivation route
        families are stamped dead; the .314 falsifier hook pins the grade.
  (ii)  the EW-floor chain carries its banked reading/seam/convention grades
        (sqrtNc carrier + bosonic reservoir [P_structural_reading]; branch
        incidence + floor measure [P_structural_seam]; hierarchy mechanism +
        anchor consistency [P_structural_convention]); the functional FORM
        v_H/M_Pl is proven not base-derivable (the .314 no-go) -- adopted,
        corroborated, irreducible.
  (iii) the M_Z/v_H factor is tree-level: the physical on-shell M_Z export
        is fenced OPEN (ew_sector_closure), so form (a) is exact only at the
        trace/tree layer; the +0.32% is a named residual, not noise.
  (iv)  one-loop running is the principled form per
        T_capacity_coupling_is_leading_log (the ledger furnishes leading-log
        content; no two-loop terms AND no top/Higgs/W threshold matching have
        ledger representation; the observed residual of everything neglected
        is +0.13 percent; one-loop empirically MORE accurate at the
        crossing). Stated for honesty: if the capacity
        crossing were instead read as a two-loop scheme value (47.02 ->
        46.11), t would shift by -1.10 nats (factor 0.33 in M_cross) -- the
        leading-log identification is load-bearing and named.
  (v)   M_Pl remains the one external dimensional anchor (route-b); this
        module derives the RATIO only.
  (vi)  the w-propto-g^2 dictionary-index reading: t is dictionary-index-
        conditional in its very formulation (t(n) = 69.0 / 34.6 / 12.4 under
        w propto g^1/g^2/g^4 on identical native inputs, factor ~5.6;
        check_T_mcross_gate_dictionary_conditional, v24.3.364); the
        composition is stated in coupling space under the n=2 reading.

HONEST NON-CLAIMS (export flags, all pinned):
  * does NOT derive the absolute Planck magnitude (no-go stands);
  * does NOT upgrade sin^2 theta_W = 3/13 toward the measured-angle [P]
    (the w-g^2 dictionary index is content-blind, v24.3.364; the M_cross
    scale gate is NESTED inside the dictionary seam -- this composition
    operates GIVEN both readings and moves neither);
  * does NOT close the rank-1 support-exhaustiveness reading;
  * does NOT claim M_Pl/M_cross ~ 127 means anything (numerically near
    1/alpha_em; no forward computation produces that identification;
    coincidence, disclaimed).

FALSIFIER HOOKS (mutations that must fail, computed live):
  * the 45-support reading (1/alpha_Y(M_cross) = 45, the [Y != 0] indicator)
    gives t = 40.37, alpha_s(mu*) = 0.49 (413 sigma from PDG) and
    M_cross = 2.7 * M_Pl -- a SUPER-Planckian crossing, ordering-inverted;
  * a one-percent perturbation of the 3/13 angle moves alpha_s(mu*) to
    0.111 (7.6 sigma from PDG) -- the composition is rigid, not tunable.

[P_structural_reading]; tier 4; no measured target consumed (measured M_Z and
PDG alpha_s appear only as labelled comparators, never in a pass gate on the
FORM-(a) composition itself; the form-(b) window and the tree-vs-physical gap
gate are corroboration gates against the measured M_Z, banked-precedent shape,
and consume it as comparator target only -- the alpha_s comparator gates only
the REPRODUCTION of the already-banked 0.11-sigma corroboration).
"""
from __future__ import annotations

import math

from apf.apf_utils import check, _result

# --- banked capacity inputs (all fixed elsewhere; the ONLY composition inputs) ---
C_TOTAL = 61                  # capacity ledger [rigid] (L_count)
D_EFF = 102                   # (C_total-1)+C_vacuum (L_self_exclusion, T11)
SIGMA = math.log(D_EFF)       # the unique intensive quantum (L_sigma_intensive)
B2, B3, BY = 19.0 / 6.0, 7.0, 41.0 / 6.0   # SM-norm one-loop |beta| (L_beta_capacity,
                                            # T_gauge_beta_capacity_tiling_abelian)
SIN2_W = 3.0 / 13.0           # T_sin2theta / T_ew_load_placement_P
N_C = 3                       # Theorem_R, T_gauge [P]
C_BOSON = 16                  # ew_bosonic_enforcement_reservoir
LIFT = 12.0 / 7.0             # ew_branch_incidence_density
M_PL = 1.220910e19            # GeV; the ONE external dimensional anchor (route-b)

# --- comparators (labelled; never consumed by the FORM-(a) composition; the
# form-(b) window and the gap gate consume M_Z_MEASURED as comparator target only) ---
M_Z_MEASURED = 91.1876        # physical on-shell M_Z (external_input, ew_sector_closure)
ALPHA_S_PDG, ALPHA_S_SIG = 0.1180, 0.0009

EXPORT_FLAGS = dict(
    Export_mcross_planck_ratio_pure_capacity_composition_P=1,   # the collapse (form a)
    Export_mcross_ratio_rescaling_invariant_P=1,                # consistency w/ single-anchor no-go
    Export_mcross_absolute_scale_derived=0,                     # GeV value still rides route-b M_Pl
    Export_sin2theta_measured_angle_upgraded=0,                 # dictionary seam untouched (v24.3.364)
    Export_rank1_support_reading_closed=0,                      # .284 exhaustiveness stays OPEN
    Export_physical_onshell_MZ_derived=0,                       # tree only; EW loops fenced OPEN
    Export_two_loop_transport_claimed=0,                        # leading-log identity is the reading
    Export_mpl_over_mcross_alpha_em_identified=0,               # ~127 coincidence DISCLAIMED
    measured_target_consumed=0,
    target_consumed=0,
)


def _forward_t():
    """The zero-input forward solve, recomputed independently (a genuine
    cross-check of abelian_coupling_capacity_count._alpha_s_forward -- same
    inputs, independent arithmetic, no import). Returns (t, inv_a2, inv_aY)."""
    K = (C_TOTAL / 6.0) * SIGMA                 # 1/alpha_cross = S_dS/6
    ratio = (1.0 - SIN2_W) / SIN2_W             # = 10/3
    t = (ratio * K - C_TOTAL) / ((BY + ratio * B2) / (2.0 * math.pi))
    inv_a2 = K - (B2 / (2.0 * math.pi)) * t
    inv_aY = C_TOTAL + (BY / (2.0 * math.pi)) * t
    return t, inv_a2, inv_aY


def _t_at_support(invY_cross):
    """The forward solve with the abelian crossing count as a free slot
    (falsifier instrumentation: 61 = the banked rank-1 reading; 45 = the
    charged-subset [Y != 0] mutation that must fail)."""
    K = (C_TOTAL / 6.0) * SIGMA
    ratio = (1.0 - SIN2_W) / SIN2_W
    return (ratio * K - invY_cross) / ((BY + ratio * B2) / (2.0 * math.pi))


def _v_H_over_M_Pl():
    """The banked EW floor (planck_magnitude_single_anchor): pure capacity number."""
    return math.sqrt(N_C) / (4.0 * math.pi) * D_EFF ** (-(C_BOSON / 2.0)) * LIFT


def _mz_over_vH(inv_a2):
    """Tree-level M_Z/v_H = sqrt(g^2+g'^2)/2 at the solve's endpoint. Because
    the endpoint satisfies 1/alpha_Y = (10/3)(1/alpha_2) exactly, this is
    sqrt(13 pi / (10 / alpha_2^-1)) in closed form (SM non-GUT normalization)."""
    return math.sqrt(13.0 * math.pi / (10.0 * inv_a2))


def _ratio_composed(m_pl=M_PL):
    """M_cross/M_Pl (form a) -- note m_pl cancels; the argument exists only for
    the rescaling-invariance witness."""
    t, inv_a2, _ = _forward_t()
    v_ratio = (_v_H_over_M_Pl() * m_pl) / m_pl          # = f, lambda-independent
    return math.exp(t) * _mz_over_vH(inv_a2) * v_ratio


def check_T_mcross_planck_ratio_composition():
    # -- 1. the forward solve, recomputed independently --
    t, inv_a2, inv_aY = _forward_t()
    check(abs(t - 34.592316) < 1e-4,
          f"t = ln(M_cross/mu*) = {t:.6f} (zero measured coupling; matches the "
          "banked forward solve, recomputed independently)")
    # exact closed form: t = (4 pi / 313)(305 ln 102 - 549)
    t_closed = (4.0 * math.pi / 313.0) * (305.0 * math.log(102) - 549.0)
    check(abs(t - t_closed) < 1e-9,
          "t = (4pi/313)(305 ln 102 - 549) exactly -- a pure capacity expression")
    # endpoint self-consistency: the angle at mu* is exactly 3/13, and the
    # composed 1/alpha_em comes OUT (never in)
    check(abs(inv_a2 / (inv_a2 + inv_aY) - SIN2_W) < 1e-12,
          "sin^2 theta_W(mu*) = 3/13 exactly at the solve's own endpoint")
    check(abs((inv_a2 + inv_aY) - 128.2075) < 5e-3,
          f"1/alpha_em(mu*) = {inv_a2 + inv_aY:.4f} reproduces L_alpha_em's "
          "derived 128.2075 as an internal cross-formula identity, "
          "content-equivalent to the alpha_s gate (shared one-loop system); "
          "world value PDG-2024 127.930, 0.22 percent")
    # the banked 0.11-sigma corroboration reproduces
    alpha_s = 1.0 / ((C_TOTAL / 6.0) * SIGMA - (B3 / (2.0 * math.pi)) * t)
    check(abs(alpha_s - ALPHA_S_PDG) / ALPHA_S_SIG < 0.2,
          f"alpha_s(mu*) = {alpha_s:.5f} reproduces the banked 0.11-sigma "
          "corroboration (comparator, not an input)")

    # -- 2. the composition, form (a): pure capacity end to end --
    f_vH = _v_H_over_M_Pl()
    mz_vh = _mz_over_vH(inv_a2)
    ratio = math.exp(t) * mz_vh * f_vH
    m_cross_a = ratio * M_PL
    check(7.85e-3 < ratio < 7.96e-3,
          f"M_cross/M_Pl = {ratio:.4e} -- a pure capacity number (form a)")
    check(9.60e16 < m_cross_a < 9.70e16,
          f"M_cross = {m_cross_a:.3e} GeV (form a; GeV value rides the one "
          "route-b anchor, as every mass in the scale-web does)")

    # -- 3. form (b): the measured-M_Z comparator bracket --
    m_cross_b = M_Z_MEASURED * math.exp(t)
    check(9.57e16 < m_cross_b < 9.67e16,
          f"comparator form (b): e^t * M_Z(measured) = {m_cross_b:.3e} GeV")
    gap = m_cross_a / m_cross_b - 1.0
    check(0.001 < gap < 0.006,
          f"form (a)/(b) gap = {100*gap:.2f}% = the tree-vs-physical M_Z residual "
          "(EW radiative layer; physical on-shell M_Z export fenced OPEN, "
          "ew_sector_closure)")

    # -- 4. rescaling invariance: consistency WITH the single-anchor no-go --
    for lam in (1.0, 2.0, 7.3, 1e-5, 1e8):
        check(math.isclose(_ratio_composed(M_PL * lam), ratio, rel_tol=1e-12),
              f"M_Pl -> lambda*M_Pl (lambda={lam}) leaves M_cross/M_Pl fixed: "
              "the composition derives a RATIO, no absolute scale")

    # -- 5. falsifier hooks (mutations computed live, must fail physically) --
    t45 = _t_at_support(45)
    alpha_s_45 = 1.0 / ((C_TOTAL / 6.0) * SIGMA - (B3 / (2.0 * math.pi)) * t45)
    inv_a2_45 = (C_TOTAL / 6.0) * SIGMA - (B2 / (2.0 * math.pi)) * t45
    m_cross_45 = math.exp(t45) * _mz_over_vH(inv_a2_45) * f_vH * M_PL
    check(alpha_s_45 > 0.4,
          f"FALSIFIER: the 45-support ([Y!=0]) mutation gives alpha_s = "
          f"{alpha_s_45:.3f} -- {abs(alpha_s_45-ALPHA_S_PDG)/ALPHA_S_SIG:.0f} "
          "sigma from PDG, absurd")
    check(m_cross_45 > M_PL,
          f"FALSIFIER: the 45-support mutation puts M_cross = "
          f"{m_cross_45/M_PL:.2f} * M_Pl -- SUPER-Planckian, ordering-inverted; "
          "the 61-count is the only physical support reading of the family")
    # rigidity: a 1% angle perturbation breaks the alpha_s corroboration (>3 sigma)
    s2p = SIN2_W * 1.01
    rp = (1.0 - s2p) / s2p
    K = (C_TOTAL / 6.0) * SIGMA
    tp = (rp * K - C_TOTAL) / ((BY + rp * B2) / (2.0 * math.pi))
    alpha_s_p = 1.0 / (K - (B3 / (2.0 * math.pi)) * tp)
    check(abs(alpha_s_p - ALPHA_S_PDG) / ALPHA_S_SIG > 3.0,
          "FALSIFIER: a 1% perturbation of 3/13 moves alpha_s to "
          f"{alpha_s_p:.4f} ({abs(alpha_s_p-ALPHA_S_PDG)/ALPHA_S_SIG:.1f} sigma) "
          "-- the composition is rigid, not tuned")

    # -- 6. honest non-claims, pinned --
    check(EXPORT_FLAGS["Export_mcross_absolute_scale_derived"] == 0,
          "NOT claimed: the absolute scale (the single-anchor + .314 no-gos stand; "
          "GeV values consume the route-b anchor)")
    check(EXPORT_FLAGS["Export_sin2theta_measured_angle_upgraded"] == 0,
          "NOT claimed: any movement of the measured-angle reading (the w-g^2 "
          "dictionary index is content-blind, v24.3.364; the scale gate is "
          "nested inside the dictionary seam)")
    check(EXPORT_FLAGS["Export_rank1_support_reading_closed"] == 0,
          "NOT claimed: closure of the .284 rank-1 support-exhaustiveness reading "
          "(five route families stamped dead; taken as given at "
          "[P_structural_reading])")
    check(EXPORT_FLAGS["Export_physical_onshell_MZ_derived"] == 0,
          "NOT claimed: the physical on-shell M_Z (tree/trace level only; the EW "
          "loop layer stays with ew_sector_closure's open boundary)")
    check(EXPORT_FLAGS["Export_mpl_over_mcross_alpha_em_identified"] == 0,
          f"NOT claimed: M_Pl/M_cross = {M_PL/m_cross_a:.1f} ~ 1/alpha_em is a "
          "numerical coincidence, disclaimed by construction")
    check(EXPORT_FLAGS["measured_target_consumed"] == 0,
          "no measured target consumed by the composition")

    return _result(
        name=("T_mcross_planck_ratio_composition: M_cross/M_Pl composes to a pure "
              "capacity number under the named readings -- e^t * (M_Z/v_H)_tree * (v_H/M_Pl) = 7.905e-3 "
              "(M_cross = 9.65e16 GeV) with t = (4pi/313)(305 ln 102 - 549) = "
              "34.5923 recomputed independently from {61, ln 102, 3/13, betas}, "
              "M_Z/v_H = sqrt(13pi/(10/alpha_2^-1)) tree-level, and the banked EW "
              "floor. Zero measured couplings; rescaling-invariant (ratio only); "
              "the 45-support mutation fails super-Planckian. Closes the 'lone "
              "un-collapsed ratio' surface [P_structural_reading]"),
        tier=4,
        epistemic='P_structural_reading',
        summary=(
            "The single-anchor scale-web named M_cross/M_Pl as the lone "
            "un-collapsed ratio -- a surface stale in its GRADE LABELS and "
            "its sin^2theta_W-gate identification; the 'open content = "
            "v_H/M_Pl' pin is a deliberate fence that STANDS (the .314 no-go "
            "proves the form v_H/M_Pl is not base-derivable), so this "
            "composition repackages the hierarchy at reading grade, it does "
            "not close it. v_H/M_Pl is banked "
            "(planck_magnitude_single_anchor) and the abelian rank-1 count "
            "supplies t with zero measured couplings (v24.3.215/.284). This "
            "module composes them: "
            "M_cross/M_Pl = e^t * (M_Z/v_H) * (v_H/M_Pl), where t = "
            "(4pi/313)(305 ln 102 - 549) = 34.5923 is recomputed independently "
            "from the capacity inputs (endpoint angle exactly 3/13; the "
            "endpoint alpha_s = 0.1179 comes OUT as a consequence; the "
            "endpoint 1/alpha_em = 128.21 reproduces L_alpha_em's derived "
            "value as an internal cross-formula identity, by construction of "
            "the shared one-loop system -- world comparator PDG-2024 "
            "127.930, 0.22 percent), M_Z/v_H = sqrt(13pi/(10*(1/alpha_2))) = 0.37154 is "
            "the tree-level EW mass relation at the endpoint couplings (SM "
            "non-GUT normalization; tree M_Z = 91.48 GeV, +0.32% above "
            "physical -- the EW radiative layer, fenced OPEN at "
            "ew_sector_closure), and v_H/M_Pl = sqrt(3)(12/7)/(4pi) * 102^-8 "
            "is the banked EW floor. Result: M_cross/M_Pl = 7.905e-3, M_cross "
            "= 9.65e16 GeV (form a) vs the measured-M_Z comparator 9.62e16 "
            "(form b). The identification mu* = tree M_Z is definitional "
            "at tree level (its corroboration is the +0.32% gap gate against "
            "the measured M_Z) and the composition is invariant under M_Pl "
            "rescaling -- it derives the RATIO only, so the single-anchor and "
            ".314 no-gos stand untouched. GRADE [P_structural_reading], the "
            "weakest link, riders named: (i) the .284 rank-1 support reading "
            "(alpha_s-corroborated 0.11 sigma, exhaustiveness OPEN, .314-"
            "pinned); (ii) the EW-floor reading/seam/convention chain (the "
            "FORM v_H/M_Pl proven not base-derivable); (iii) tree-level M_Z "
            "(+0.32% named residual); (iv) the leading-log identity as the "
            "principled loop order (a two-loop re-reading of the crossing "
            "would shift t by -1.1 nats; no two-loop terms AND no "
            "top/Higgs/W threshold matching have ledger representation, "
            "T_capacity_coupling_is_leading_log; the observed residual of "
            "everything neglected is +0.13 percent -- named, not claimed); "
            "(v) route-b "
            "anchor for GeV; (vi) the w-propto-g^2 dictionary-index reading "
            "(t(n) = 69.0/34.6/12.4 under w propto g^1/g^2/g^4 on identical "
            "native inputs, factor ~5.6; "
            "check_T_mcross_gate_dictionary_conditional, v24.3.364) -- the "
            "composition is stated in coupling space under the n=2 reading. "
            "NON-CLAIMS pinned by export flags: no absolute "
            "scale, no measured-angle upgrade (the scale gate is nested "
            "inside the w-g^2 dictionary seam, v24.3.364), no support-reading "
            "closure, no physical M_Z, and M_Pl/M_cross ~ 127 is a disclaimed "
            "coincidence. FALSIFIERS live: the 45-support mutation gives "
            "alpha_s = 0.49 (413 sigma) and M_cross = 2.7 M_Pl "
            "(super-Planckian); a 1% angle perturbation breaks the composed "
            "alpha_em endpoint."
        ),
        key_result=(
            "M_cross/M_Pl = e^t * sqrt(13pi/(10/alpha_2^-1)) * sqrt(3)(12/7)/(4pi) "
            "* 102^-8 = 7.905e-3 -- a pure capacity number under the named "
            "readings, zero measured input; "
            "M_cross = 9.65e16 GeV (form a) / 9.62e16 (measured-M_Z comparator). "
            "t = (4pi/313)(305 ln 102 - 549). The 'lone un-collapsed ratio' "
            "surface closes; grade [P_structural_reading] (weakest link: the .284 "
            "rank-1 reading + the EW-floor readings); the no-gos stand."
        ),
        dependencies=[
            'T_abelian_coupling_fixed_by_rank1_capacity_count',
            'T_sin2theta',
            'L_beta_capacity',
            'T_gauge_beta_capacity_tiling_abelian',
            'L_crossing_entropy',
            'L_sigma_intensive',
            'T_planck_magnitude_single_dimensional_anchor',
            'T_fermion_strong_no_new_dimensional_anchor',
            'T_ew_sqrtNc_carrier_forced_by_color_triplet_trace',
            'T_ew_scale_functional_independence_no_go',
            'T_capacity_coupling_is_leading_log',
        ],
        artifacts=dict(
            composition="M_cross/M_Pl = e^t * (M_Z/v_H)_tree * (v_H/M_Pl)",
            t_closed_form="t = (4pi/313)(305 ln 102 - 549) = 34.592316",
            t_value=round(t, 6),
            inv_alpha_2_endpoint=round(inv_a2, 6),
            inv_alpha_Y_endpoint=round(inv_aY, 6),
            inv_alpha_em_endpoint=round(inv_a2 + inv_aY, 6),
            mz_over_vH_tree=round(mz_vh, 9),
            tree_MZ_GeV=round(mz_vh * f_vH * M_PL, 4),
            vH_over_MPl=f"{f_vH:.6e}",
            ratio_form_a=f"{ratio:.6e}",
            M_cross_form_a_GeV=f"{m_cross_a:.4e}",
            M_cross_form_b_GeV=f"{m_cross_b:.4e}",
            tree_vs_physical_MZ_gap_pct=round(100 * gap, 3),
            falsifier_45_support=dict(t=round(t45, 4), alpha_s=round(alpha_s_45, 4),
                                      M_cross_over_MPl=round(m_cross_45 / M_PL, 3)),
            coincidence_disclaimed=f"M_Pl/M_cross = {M_PL/m_cross_a:.2f} (NOT 1/alpha_em)",
            rider_ledger=("(i) .284 rank-1 support reading; (ii) EW-floor "
                          "reading/seam/convention chain + .314 form no-go; "
                          "(iii) tree-level M_Z (+0.32%); (iv) leading-log loop "
                          "order (no two-loop terms and no top/Higgs/W "
                          "threshold matching have ledger representation; "
                          "observed residual of everything neglected +0.13%); "
                          "(v) route-b anchor for GeV; (vi) the w-propto-g^2 "
                          "dictionary-index reading (t(n) = 69.0/34.6/12.4, "
                          "factor ~5.6; "
                          "check_T_mcross_gate_dictionary_conditional, "
                          "v24.3.364) -- stated in coupling space under the "
                          "n=2 reading"),
            export_flags=dict(EXPORT_FLAGS),
        ),
    )


_CHECKS = {
    "T_mcross_planck_ratio_composition": check_T_mcross_planck_ratio_composition,
}


def register(registry):
    registry.update(_CHECKS); return registry


def run_all():
    return {name: fn() for name, fn in _CHECKS.items()}


if __name__ == "__main__":
    r = check_T_mcross_planck_ratio_composition()
    print("PASS" if r["passed"] else "FAIL")
    for k, v in r["artifacts"].items():
        print(f"  {k}: {v}")


# ---------------------------------------------------------------------------
# Interface Engine onboarding (Full Bank Onboarding wave, 2026-07-04)
# ---------------------------------------------------------------------------

IE_DECLARATIONS = (
    {
        "input_id": "ew:mcross_planck_ratio_reading",
        "axis": "ROUTE",
        "expect_export": False,
        "claim_text": (
            "M_cross/M_Pl composes to a pure capacity number under NAMED "
            "READINGS [P_structural_reading] with six riders: "
            "M_cross/M_Pl = e^t (M_Z/v_H)(v_H/M_Pl), every factor banked; "
            "t = (4 pi / 313)(305 ln 102 - 549) = 34.592316 zero-input; "
            "M_cross = 9.65e16 GeV. The composition REPACKAGES the "
            "hierarchy at reading grade -- the .314 no-go proves v_H/M_Pl "
            "is not base-derivable, so it does NOT close it; promotion to "
            "bare [P] is FORECLOSED by the wg2 dictionary fence "
            "(index-conditional M_cross gate); the near-integer "
            "M_Pl/M_cross ~ 127 is NOT a claim and is never numerologized. "
            "(check_T_mcross_planck_ratio_composition, "
            "mcross_planck_ratio_composition.py)"
        ),
        "note": (
            "Onboards the .365 M_cross collapse onto the ROUTE axis as a "
            "held claim at [P_structural_reading]. The never-promote and "
            "never-numerologize clauses are frozen in the claim text; a "
            "future lane exporting the ratio at bare [P] fails the pin."
        ),
    },
)
