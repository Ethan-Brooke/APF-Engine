"""Operational completeness of the interface observer + the local-removability converse.

The access-partition arc (2026-06-09; reference notes 'E=mc2 as the Access Partition of
the Accumulated Ledger' + 'L_local_removability - The Converse Lemma from the Ledger-Line
Decomposition', both cold-audited REDUCES with corrections applied) reduced the shared
[P_structural] seam under photon-masslessness, the YM record-locking dictionary, and the
C_4 keystone to ONE operational clause plus the (separately fenced) spectrum export. This
module gives that clause and its converse lemma executable bank content.

THE CLAUSE (L_operational_completeness): the operation set available to the observer at
interface Gamma is EXACTLY the set of admissibility-data REMOVALS billed solely at
Gamma (within global admissibility). (Removals narrowing per the 2026-06-09
[P]-promotion audit: row 6 licenses removals; additions/general modifications are
not claimed; the executable op set was deletions-only from v24.3.230.) Forced from both sides -- the SANDWICH:

  UPPER BOUND (L_loc): no observer can modify admissibility data billed at another
    interface, and no observer can perform the SIMULTANEOUS multi-interface modification
    a cross-billed joint line requires (L_irr check 4's operative argument). Ops cannot
    exceed own-billing modifications.
  LOWER BOUND (the necessity structure of L_irr's two countermodels, made executable
    here): L_irr [P] proves the necessity of the occupancy input (Delta>0) and L_loc via two countermodels whose
    conclusions ('additive world: all capacity locally recoverable, fully reversible';
    'single-interface world: observer has global access, all correlations recoverable')
    REQUIRE own-billing modifications to be available -- and the two countermodels
    force the two HALVES of the clause separately, both executed here: withholding all
    deletions makes the additive world irreversible at Delta=0 (proving irreversibility
    WITHOUT superadditivity, collapsing the necessity-of-L_nc argument -- CM1 forces
    the footprint half); withholding only joint-line deletions leaves the additive
    world fully reversible but makes the single-interface world irreversible
    (contradicting CM2's 'all correlations are recoverable' -- CM2 forces the
    single-billed-joint half). So any operation set smaller than own-billing
    modifications contradicts the banked countermodel structure at one half or the
    other; any larger violates L_loc. Equality is forced, executed at both halves.

Previously the clause lived as PROSE: check 4's "By L_loc, an observer at Gamma_S can
only modify admissibility at Gamma_S" (the 'only' presupposing the 'can') and the two
countermodels' conclusions -- countermodel 2's executable body was a literal True. This
module replaces that prose-level license with executed countermodel bodies (per the
principal's 2026-06-09 option-(b) ruling).

PRINCIPAL RULING RECORDED (2026-06-09, Q1 of the access-partition review): the standing-
state energy usage inside T_thermodynamics_four_laws_synthesis [P] (E(state) = n*eps
witnessed per state alongside W = d_eff^n and S = n*sigma) is IN-SCOPE -- 'accumulated
cost' is a path-independent state function (L_cost is exact and linear), with the second
law's ratchet (n non-decreasing) carried by the LOCKED sub-ledger. The cold-audit flag
'latent scope tension in the four-laws check' is thereby adjudicated and closed; no
change to the four-laws check is needed.

THE CONVERSE LEMMA (L_local_removability, billing-locus form, v0.2 post-audit): a
distinction's committed capacity is recoverable by local operations IFF no joint
(Moebius) line containing it is billed at >= 2 interfaces. Forward = L_irr [P] (the
simultaneity obstruction). Backward = the clause: footprint lines are deletable by their
owner (countermodel-1 half); single-billed joint lines are deletable by their sole
owner, who has access to all correlates at that interface (countermodel-2 half); every
intermediate state admissible by L3 monotonicity. Corollary (the E=mc2 chain's B3<-):
the operational floor of a record class equals the locked-closure energy -- the
multi-billed joint terms plus the pinned footprints of the record's support -- and a
Delta_cross=0 class strips to the vacuum (floor 0). Hence: operational floor > 0 iff
the class holds a record. Mass is the locked closure; masslessness is operational
(at the ledger level; the spectrum export is separately fenced).

THE TRANSLATION LAYER (L_recoverability_is_orbit_reachability, third check): the
Route C cold audit named the chain's sharpest residue -- the unnamed translation
between cost-ledger recoverability and P_Gamma-orbit reachability. The translation-
gate note ('Reference - The Translation Named - Recoverability and P_Gamma-Orbit
Reachability (2026-06-09).md', cold-audited REDUCES-WITH-CORRECTIONS, corrections
applied) decomposed it into five steps: three are theorems executed in the third
check below (action-locality derived; removals-only WLOG; cross-interface composition
dissolved into the product), and two are consumptions of canonical Paper 0 text.
THE PRINCIPAL LANDED THE PAYMENT (2026-06-09 land-all-three ruling): Paper 0 v6.2.27
sec:perturbations prose now carries row 6 (operational completeness,
removals-only), row 7a (threat-totality), and row 7b (defense-billing locus), plus
falsifier (iv). The license basis of this module's clause thereby upgrades from
countermodel-prose sandwich to canonical-definition citation with named published
rows; the sandwich and the executable CM bodies remain as corroboration.

THE [P]-PROMOTION (2026-06-09, dedicated fresh-context grade audit; verdict appended
to the translation-gate reference note): PROMOTE all three checks to [P], conditions
applied in this version. Controlling precedent: check_L_epsilon_star carries bare [P]
over the adopted-with-falsifier structural conditions MD/BW, and check_P4_IMP carries
[P] over zero-translation sec:perturbations row consumption -- '[P] = proved within the
canonically adopted basis, adopted commitments as named dependencies' is what the tag
has always meant in this bank. Conditions: (1) the clause narrowed to REMOVALS (row 6's
actual license); (2) the closure identity scoped to the record-support-closed class
(the >= 3-interface fusion fork at that time named-not-decided; DISSOLVED 2026-06-10,
fourth check below); (3) the MD support condition
restated as a DERIVATION (FD3 + MD + row 7b), not a new identification; (4) LICENSE-
CLASS GUARD: this precedent extends ONLY to interface-ontology rows audited as fair
readings of canonical definitional text (what P_Gamma contains, what T(d) covers,
where bills land -- the FD4/FD5b/MD stratum); physical-observable DICTIONARY rows
(e.g. any 'coupling = enforcement amplitude' row) do NOT inherit it, regardless of
canonization -- the named blocked route is w proportional to g^2 -> sin^2 theta_W at
[P], which the campaign fences forbid. First clean same-day-row consumption at [P] in
the bank's history; precedent deliberately established, guard attached. Composed
ledger-level B3<-: qualitative (floor > 0 iff record) [P]; quantitative closure
identity [P] on the support-closed class; spectrum export [P_structural] by design
and untouched.

THE FUSION-SCOPE DISSOLUTION (T_fusion_scope_dissolved, fourth check, 2026-06-10):
the X8 fork -- does a record pin its members' footprints at interfaces where the
record is not billed? -- is a NAME COLLISION, not an open decision. The all-bits and
billing-scope "arms" are ONE operator semantics applied to TWO pinned-fact classes:
the all-bits floor is the billing-scope floor of the class that pins the record AND
(world-globally -- the 2026-06-10 cold audit's correction; the record-local form is
FALSE at |U| = 4) every held cross-billed fact's members' foreign holdings. Which
pin-set deserves the name "the record class" is settled by canonical text: row 7b's
line-level instance (a Gamma-billed correlation is a correlation among the
Gamma-aspects of its members -- Paper 0 v6.2.27, verbatim) + FD1/FD3 (the only
cross-interface objects in the bookkeeping are the multiply-billed correlations).
The record class pins the record FACT; E_rest(record) is the billing-scope value;
the all-bits value is the correct floor of the larger pinned class. The closure
identity, stated per-interface (where the first law lives: dE = dQ + dW at any
interface), is single-valued at EVERY interface count -- the fork's three-interface
threshold was an artifact of the disjoint-cover model. Third instance of one
disease, same cure: V2a (state space), W5 (activation predicate), X8 (class
definition) -- per-interface honesty each time. GRADE [P] (landed
[P_structural] v24.3.233 per the principal's two-step ruling; PROMOTED v24.3.234 by
the dedicated fresh-context audit, PROMOTE-WITH-CONDITIONS, conditions applied: the
Paper 0 ~2981 landing-paragraph sentence named as part of the row-7b consumption --
"the only cross-interface objects in the bookkeeping are the multiply-billed
correlations" closes the class-naming analytically; world-global form preserved;
license-class guard restated, dictionary rows do not inherit; the all-bits floor
stays a correct floor of its own class). The composed E_rest quantitative identity
now carries [P] at every interface count for record-fact classes, named-dependent
on rows 6/7a/7b; E_rest is a LEDGER quantity -- the spectrum export stays fenced.
Reference: 'Reference - The Fusion-Scope Fork Dissolved - One Semantics, Two
Classes (2026-06-10).md'.

SCOPE / HONEST NON-CLAIMS.
  * L_operational_completeness grade: see the check (set after the 2026-06-09 cold audit
    of the sandwich). The lower bound rests on reading the countermodels' reversibility
    conclusions as OPERATIONAL claims (recoverability via available operations), which
    is what their role in L_irr's necessity argument requires; the audit interrogates
    exactly that reading.
  * L_local_removability grade [P_structural]: composes the clause + L_irr [P] + L3; the
    structural identification is the billing-locus reading of check 4's simultaneity
    obstruction (joint line billed at >= 2 interfaces == requires coordinated
    multi-interface modification).
  * NEITHER check claims the spectrum export: identifying the ledger operational floor
    with a physical Hamiltonian gap stays behind the OS2/ML dictionary and
    T_no_physical_time_flow_overclaim, [P_structural] by design.
  * NEITHER check moves sin^2 theta_W or any EW quantity; the campaign hub's fences
    stand. No numerical prediction is made or altered.

Pure arithmetic (fractions + itertools); no scipy; the module always loads.
"""
from __future__ import annotations
import itertools
from fractions import Fraction as F

from apf.apf_utils import check as _check, _result as _full_result


# ---------------------------------------------------------------------------
# Moebius ledger-line layer (exact inclusion-exclusion; pure bookkeeping)
# ---------------------------------------------------------------------------
def _moebius(E, universe):
    """Coefficients j(T), T nonempty, with E(S) = sum_{T subset S} j(T); unique."""
    j = {}
    for r in range(1, len(universe) + 1):
        for T in itertools.combinations(sorted(universe), r):
            T = frozenset(T)
            j[T] = E[T] - sum(j[U] for U in j if U < T)
    return j


class _LedgerWorld:
    """Multi-interface cost world with the clause's operation set.

    Operation set at interface g (THE CLAUSE): delete any nonzero ledger line billed
    solely at g (footprint or joint), target state globally admissible. Multi-billed
    joint lines are deletable by no single observer (L_irr check 4 simultaneity).
    op_set='none'/'footprint_only' model the withheld-clause probes (lower bound)."""

    def __init__(self, tables, cap, universe, op_set="clause"):
        """op_set: 'clause' (own-billing modifications, the lemma's operation set),
        'footprint_only' (joint-line deletions withheld -- the CM2-necessity probe),
        'none' (all deletions withheld -- the CM1-necessity probe)."""
        self.universe = frozenset(universe)
        self.j = {g: _moebius(E, universe) for g, E in tables.items()}
        self.cap = cap
        self.op_set = op_set

    def energy(self, g, S):
        return sum(v for T, v in self.j[g].items() if T <= S)

    def total_energy(self, S):
        return sum(self.energy(g, S) for g in self.j)

    def admissible(self, S):
        return all(self.energy(g, S) <= self.cap for g in self.j)

    def line_owners(self, T):
        return [g for g in self.j if self.j[g].get(T, 0) != 0]

    def removal_is_local(self, d, S):
        """Is the state change S -> S\\{d} performable by single-interface observers
        under the clause? Yes iff every nonzero deleted line is single-billed."""
        if self.op_set == "none":
            return False
        for T in {T for g in self.j for T, v in self.j[g].items()
                  if v != 0 and d in T and T <= S}:
            if len(T) >= 2:
                if self.op_set == "footprint_only":
                    return False  # CM2-necessity probe: joint deletions withheld
                if len(self.line_owners(T)) >= 2:
                    return False
        return self.admissible(S - {d})

    def delta_cross_zero(self, d):
        """Billing-locus coupling: no joint line containing d billed at >=2 interfaces."""
        joints = {T for g in self.j for T, v in self.j[g].items()
                  if v != 0 and d in T and len(T) >= 2}
        return all(len(self.line_owners(T)) < 2 for T in joints)

    def reachable_floor(self, S0, record_part):
        """Min total energy over states reachable from S0 via clause operations,
        staying in the record class (record part held)."""
        seen, frontier = {S0}, [S0]
        best = self.total_energy(S0)
        while frontier:
            S = frontier.pop()
            best = min(best, self.total_energy(S))
            for d in S - record_part:
                if self.removal_is_local(d, S):
                    T = S - {d}
                    if T not in seen:
                        seen.add(T)
                        frontier.append(T)
        return best


# The bank's own L_irr witness tables (core.py, verbatim) ---------------------
_U = {0, 1, 2}                      # s, e, c
_CAP = F(15)
_ES = {frozenset(): F(0), frozenset({0}): F(4), frozenset({1}): F(2), frozenset({2}): F(3),
       frozenset({0, 1}): F(7), frozenset({0, 2}): F(10), frozenset({1, 2}): F(6),
       frozenset({0, 1, 2}): F(15)}
_EE = {frozenset(): F(0), frozenset({0}): F(2), frozenset({1}): F(4), frozenset({2}): F(3),
       frozenset({0, 1}): F(7), frozenset({0, 2}): F(6), frozenset({1, 2}): F(10),
       frozenset({0, 1, 2}): F(15)}
# L_irr countermodel-1 additive table (core.py, verbatim) + its E-side mirror
_ES_ADD = {frozenset(): F(0), frozenset({0}): F(4), frozenset({1}): F(2), frozenset({2}): F(3),
           frozenset({0, 1}): F(6), frozenset({0, 2}): F(7), frozenset({1, 2}): F(5),
           frozenset({0, 1, 2}): F(9)}
_EE_ADD = {frozenset(): F(0), frozenset({0}): F(2), frozenset({1}): F(4), frozenset({2}): F(3),
           frozenset({0, 1}): F(6), frozenset({0, 2}): F(5), frozenset({1, 2}): F(7),
           frozenset({0, 1, 2}): F(9)}


def _all_states(universe):
    out = []
    for r in range(len(universe) + 1):
        for c in itertools.combinations(sorted(universe), r):
            out.append(frozenset(c))
    return out


def check_L_operational_completeness():
    """L_operational_completeness: the interface observer's operation set is exactly the
    own-billing REMOVALS -- forced by the sandwich (L_loc above, the executable
    countermodel structure of L_irr below). Additions/general modifications are not
    claimed (row 6 licenses removals; 2026-06-09 [P]-promotion audit narrowing).

    Witness fields:
      1. Moebius exactness on the bank's own L_irr tables (bookkeeping layer sound).
      2. CM1 EXECUTABLE (was prose): the additive two-interface world is fully
         reversible UNDER THE CLAUSE -- every admissible state strips to the vacuum via
         own-billing deletions, globally admissible at every step (L3). 'All capacity is
         locally recoverable' is now a computed fact, not an asserted one.
      3. CM2 EXECUTABLE (was check(True)): the single-interface coupled world -- the
         observer owns every line (global access), so all correlations, including joint
         terms, are recoverable; the world is fully reversible under the clause.
      4. LOWER BOUND LOAD-BEARING, footprint half (CM1): withholding the clause makes
         CM1's world irreversible (positive operational floor with zero
         superadditivity) -- irreversibility without L_nc, contradicting L_irr's
         necessity-of-L_nc argument.
      4b. LOWER BOUND LOAD-BEARING, joint half (CM2): a footprint-only op set leaves
         CM1 intact (additive floor 0) but breaks CM2 (single-interface coupled floor
         > 0, contradicting 'all correlations are recoverable') -- the single-billed
         joint half is forced by CM2 alone. Together with field 4: any operation set
         smaller than own-billing breaks the banked countermodel structure at one half
         or the other, EXECUTED at both halves (2026-06-09 module audit, correction 1).
      5. UPPER BOUND CONSISTENT: under the clause, the coupled world's cross-billed
         correlation c remains locally unrecoverable (the check-4 lock is preserved;
         the clause does NOT overshoot L_loc).

    GRADE [P] (2026-06-09 [P]-promotion audit): lower bound = Paper 0 v6.2.27
    sec:perturbations row 6 (operational completeness, removals-only) --
    a canonical published definitional commitment consumed at zero translation, with
    falsifier (iv) exposure; the availability reading is internal to the row's landed
    text ("consumed as a lower bound on availability"). Upper bound = L_loc [P] +
    L_irr [P] (check-4 simultaneity). Precedent class: check_L_epsilon_star [P] over
    the adopted structural conditions MD/BW; check_P4_IMP [P] over sec:perturbations row 1.
    The countermodel sandwich (CM1/CM2 executable bodies) is retained as
    corroboration, no longer the license. Scope: removals of Gamma-billed lines
    within global admissibility; additions/general modifications not claimed.
    License class restricted to interface-ontology rows audited as fair readings of
    canonical definitional text; physical-dictionary rows do not inherit this
    license. Dependencies: A1, L_nc, L_loc, L_irr, Paper0_row6.
    """
    # 1. Moebius exactness on bank tables
    for E in (_ES, _EE, _ES_ADD, _EE_ADD):
        j = _moebius(E, _U)
        for S in E:
            _check(sum(v for T, v in j.items() if T <= S) == E[S],
                   "field 1: Moebius reconstruction exact on the bank's L_irr tables")

    # 2. CM1 executable: additive world fully reversible under the clause
    w_add = _LedgerWorld({"S": _ES_ADD, "E": _EE_ADD}, _CAP, _U)
    no_joint = all(len(T) < 2 or v == 0 for g in w_add.j for T, v in w_add.j[g].items())
    _check(no_joint, "field 2: additive world has zero joint lines (Delta = 0, CM1 premise)")
    for S0 in _all_states(_U):
        if not w_add.admissible(S0):
            continue
        _check(w_add.reachable_floor(S0, frozenset()) == 0,
               "field 2: CM1 executable -- every admissible state strips to the vacuum "
               "via own-billing deletions (fully reversible, computed not asserted)")

    # 3. CM2 executable: single-interface coupled world fully reversible
    w_single = _LedgerWorld({"S": _ES}, _CAP, _U)
    has_joint = any(len(T) >= 2 and v != 0 for T, v in w_single.j["S"].items())
    _check(has_joint, "field 3: single-interface world HAS joint lines (correlations present)")
    for S0 in _all_states(_U):
        if not w_single.admissible(S0):
            continue
        _check(w_single.reachable_floor(S0, frozenset()) == 0,
               "field 3: CM2 executable -- the global-access observer recovers ALL "
               "correlations (single-billed joint lines deletable by sole owner)")

    # 4. lower bound load-bearing, FOOTPRINT half (CM1): withheld clause breaks CM1
    w_off = _LedgerWorld({"S": _ES_ADD, "E": _EE_ADD}, _CAP, _U, op_set="none")
    floor_off = w_off.reachable_floor(frozenset(_U), frozenset())
    _check(floor_off > 0,
           "field 4: withholding own-billing deletion makes the ADDITIVE world "
           "irreversible (floor > 0 at Delta = 0) -- contradicts L_irr's "
           "necessity-of-L_nc countermodel; the footprint half of the lower bound "
           "is load-bearing")

    # 4b. lower bound load-bearing, JOINT half (CM2): footprint-only op set breaks
    #     CM2 but NOT CM1 -- the joint half is forced by CM2 alone (2026-06-09
    #     module audit, required correction 1)
    w_fp_single = _LedgerWorld({"S": _ES}, _CAP, _U, op_set="footprint_only")
    floor_fp_single = w_fp_single.reachable_floor(frozenset(_U), frozenset())
    _check(floor_fp_single > 0,
           "field 4b: footprint-only op set leaves the single-interface coupled world "
           "irreversible (floor > 0) -- contradicts CM2's 'all correlations are "
           "recoverable'; the joint-line half of the lower bound is forced by CM2")
    w_fp_add = _LedgerWorld({"S": _ES_ADD, "E": _EE_ADD}, _CAP, _U, op_set="footprint_only")
    _check(w_fp_add.reachable_floor(frozenset(_U), frozenset()) == 0,
           "field 4b: the additive world is fully reversible under footprint-only ops "
           "(floor 0) -- CM1 alone cannot force the joint half; the two countermodels "
           "force the two halves separately")

    # 5. upper bound consistent: the cross-billed lock survives the clause
    w_cpl = _LedgerWorld({"S": _ES, "E": _EE}, _CAP, _U)
    _check(not w_cpl.removal_is_local(2, frozenset(_U)),
           "field 5: the cross-billed correlation c stays locally unrecoverable under "
           "the clause (check-4 lock preserved; clause does not overshoot L_loc)")

    return _full_result(
        name="L_operational_completeness: the interface operation set is exactly the own-billing removals (sandwich + Paper 0 row 6)",
        tier=3,
        epistemic="P",
        summary=(
            "The operation set available to the observer at interface Gamma is exactly the set of "
            "admissibility-data REMOVALS billed solely at Gamma, within global admissibility "
            "(additions/general modifications not claimed -- row 6's license). "
            "UPPER bound: L_loc -- no cross-interface modification, no multi-interface simultaneity "
            "(L_irr check 4). LOWER bound: L_irr's two necessity countermodels, given executable "
            "bodies here (CM1 additive world: fully reversible under the clause, computed over all "
            "admissible states; CM2 single-interface world: global-access observer recovers all "
            "correlations -- replacing the prior literal-True check body); the two halves of the "
            "clause are forced SEPARATELY and both are executed: withholding all deletions breaks "
            "CM1 (additive world irreversible at Delta=0, contradicting necessity-of-L_nc); a "
            "footprint-only op set leaves CM1 intact but breaks CM2 (single-interface floor > 0). "
            "Equality forced, executed at both halves. The cross-billed lock is preserved (no overshoot). GRADE "
            "[P] per the 2026-06-09 [P]-promotion audit: lower bound = Paper 0 v6.2.27 row 6 "
            "(canonical, zero-translation, falsifier (iv)); upper bound = L_loc [P] + L_irr [P]; "
            "precedent class = check_L_epsilon_star over MD/BW + check_P4_IMP over row 1; the "
            "sandwich retained as corroboration. License class: interface-ontology rows only; "
            "physical-dictionary rows do not inherit. Records the principal's 2026-06-09 "
            "Reading-A ruling closing the four-laws standing-energy audit flag."
        ),
        key_result=(
            "Interface operation set = own-billing removals exactly (L_loc caps above; Paper 0 "
            "row 6 + executable L_irr countermodel structure force below). CM1/CM2 prose "
            "conclusions replaced by computed reversibility; withheld clause -> additive world "
            "irreversible (contradiction). [P]"
        ),
        dependencies=["A1", "L_nc", "L_loc", "L_irr", "Paper0_row6_operational_completeness"],
        cross_refs=["T_realignment_cost_is_transition_energy", "T_thermodynamics_four_laws_synthesis",
                    "T_photon_massless_from_reversibility", "T_ledger_reversibility_is_L_irr_lock"],
        artifacts={
            "sandwich": "L_loc (upper) + executable countermodel necessity (lower, both halves: CM1 footprint / CM2 joint) -> equality",
            "CM1": "additive 2-interface world: all admissible states strip to vacuum (computed)",
            "CM2": "single-interface coupled world: all correlations recovered (was check(True))",
            "lower_bound_witness": "clause withheld -> additive-world floor > 0 (irreversible at Delta=0)",
            "principal_ruling": "2026-06-09 Reading A: four-laws standing-state energy usage in-scope; flag closed",
        },
    )


def check_L_local_removability():
    """L_local_removability: committed capacity is recoverable by local operations IFF no
    joint line containing the distinction is billed at >= 2 interfaces (billing-locus
    Delta_cross = 0). Corollary: operational class floor = locked-closure energy; a
    record-free class strips to the vacuum.

    Witness fields (the post-audit v0.2 suite, on the bank's own tables + the audit
    countermodels):
      1. FORWARD on the bank's coupled tables: c (cross-billed joint lines) is not
         locally recoverable -- reproduces L_irr check 4 in the line model.
      2. BACKWARD on the bank's additive tables: every distinction Delta_cross=0,
         fully recoverable, admissible at every step (L3).
      3. FOOTPRINT-SPREAD vs COUPLING: a Delta_cross=0 distinction with footprints at
         BOTH interfaces (the bank's s: 4 at Gamma_S, 2 at Gamma_E) is recoverable --
         each owner frees their own line; spread does not lock; only cross-billing does.
      4. THE AUDITOR'S COUNTERMODEL (same-interface coupling, L_nc's single-cut shape):
         a,b coupled at Gamma_S only -- recoverable by the sole owner (countermodel-2
         half); operational floor 0; no false 'massive record-free class'. (The retired
         arity criterion would have blocked it: floor 5 with no B2 record -- the
         category error the 2026-06-09 cold audit caught.)
      5. FLOOR = LOCKED CLOSURE (mixed world): record pair (a,b) cross-billed, spectator
         l clean -- the record-class operational floor equals the closure energy
         E(record support) exactly; the spectator strips; Delta_cross=0 class floor = 0.

    GRADE [P] (2026-06-09 [P]-promotion audit): forward = L_irr [P]; backward =
    L_operational_completeness [P] (removals-only) + L3; the billing-locus reading =
    Paper 0 v6.2.27 row 7b (defense-billing locus, canonical) + the action-locality
    theorem of L_recoverability_is_orbit_reachability. The iff and the qualitative
    corollary (floor > 0 iff the class holds a record) are fusion-fork-INSENSITIVE at
    every interface count (removability pins nothing; both fork arms agree on whose
    bits fuse for the removed distinction). The quantitative closure identity
    floor == total_energy(record) is certified [P] where the pinned set covers all
    cross-billed support (condition confessed per the 2026-06-10 cold audit: on a
    world with an unpinned cross-billed line the formula reads a different class --
    394/2592 cells of the two-interface scan domain sit off the condition; this
    check's mixed-world witness satisfies it). The >= 3-interface X8 fusion fork is
    DISSOLVED (2026-06-10, T_fusion_scope_dissolved [P], promoted v24.3.234): one semantics,
    two pinned-fact classes -- the record class pins the record FACT (row 7b), so
    the record-fact closure identity is per-interface and single-valued at every
    interface count, and the all-bits value is the floor of the larger class that
    also pins held cross-billed facts' members' foreign holdings (world-global). The corollary is the
    LEDGER-level B3<- of the access-partition chain (gapped record class <-> record);
    the spectrum export stays behind OS2/ML + T_no_physical_time_flow_overclaim.
    Dependencies: A1, L_nc, L_loc, L_irr.
    """
    # 1. forward: cross-billed lock
    w_cpl = _LedgerWorld({"S": _ES, "E": _EE}, _CAP, _U)
    _check(not w_cpl.delta_cross_zero(2), "field 1: c is cross-billed (Delta_cross > 0)")
    _check(not w_cpl.removal_is_local(2, frozenset(_U)),
           "field 1: forward -- cross-billed c not locally recoverable (L_irr check 4)")

    # 2. backward: additive world
    w_add = _LedgerWorld({"S": _ES_ADD, "E": _EE_ADD}, _CAP, _U)
    S = frozenset(_U)
    for d in sorted(_U):
        _check(w_add.delta_cross_zero(d), "field 2: additive world Delta_cross = 0")
        _check(w_add.removal_is_local(d, S), "field 2: backward -- recoverable")
        S = S - {d}
        _check(w_add.admissible(S), "field 2: intermediate admissible (L3)")
    _check(S == frozenset(), "field 2: full recovery to the vacuum")

    # 3. spread vs coupling
    _check(w_add.j["S"].get(frozenset({0}), 0) != 0 and w_add.j["E"].get(frozenset({0}), 0) != 0,
           "field 3: s has footprints at BOTH interfaces (spread)")
    _check(w_add.delta_cross_zero(0) and w_add.removal_is_local(0, frozenset(_U)),
           "field 3: footprint spread does not lock; only cross-billing locks")

    # 4. the auditor's same-interface countermodel
    ES3 = {frozenset(): F(0), frozenset({0}): F(2), frozenset({1}): F(2), frozenset({2}): F(0),
           frozenset({0, 1}): F(5), frozenset({0, 2}): F(2), frozenset({1, 2}): F(2),
           frozenset({0, 1, 2}): F(5)}
    EE3 = {Sx: F(0) for Sx in ES3}
    w_same = _LedgerWorld({"S": ES3, "E": EE3}, _CAP, _U)
    _check(any(len(T) >= 2 and v != 0 for T, v in w_same.j["S"].items()),
           "field 4: same-interface joint term present (L_nc single-cut shape)")
    _check(w_same.delta_cross_zero(0) and w_same.removal_is_local(0, frozenset({0, 1})),
           "field 4: single-billed coupling recoverable by its sole owner (CM2 half)")
    _check(w_same.reachable_floor(frozenset({0, 1}), frozenset()) == 0,
           "field 4: operational floor 0 -- no massive record-free class (the retired "
           "arity criterion would have manufactured one: the audited category error)")

    # 5. floor = locked closure (mixed world: cross-billed record (a,b) + spectator l)
    ES2 = {frozenset(): F(0), frozenset({0}): F(3), frozenset({1}): F(2), frozenset({2}): F(2),
           frozenset({0, 1}): F(8), frozenset({0, 2}): F(5), frozenset({1, 2}): F(4),
           frozenset({0, 1, 2}): F(10)}
    EE2 = {frozenset(): F(0), frozenset({0}): F(2), frozenset({1}): F(3), frozenset({2}): F(1),
           frozenset({0, 1}): F(7), frozenset({0, 2}): F(3), frozenset({1, 2}): F(4),
           frozenset({0, 1, 2}): F(8)}
    w_mix = _LedgerWorld({"S": ES2, "E": EE2}, _CAP, _U)
    rec = frozenset({0, 1})
    _check(not w_mix.delta_cross_zero(0) and not w_mix.delta_cross_zero(1)
           and w_mix.delta_cross_zero(2),
           "field 5: (a,b) cross-billed record; l a clean spectator")
    floor = w_mix.reachable_floor(frozenset(_U), rec)
    closure = w_mix.total_energy(rec)
    _check(floor == closure,
           "field 5: record-class operational floor == locked-closure energy "
           f"({floor} == {closure}); spectator stripped, record support pinned")
    _check(w_mix.reachable_floor(frozenset({2}), frozenset()) == 0,
           "field 5: the record-free class strips to the vacuum (floor 0)")

    return _full_result(
        name="L_local_removability: recoverable iff no cross-billed joint line (billing-locus converse of L_irr; Paper 0 row 7b)",
        tier=3,
        epistemic="P",
        summary=(
            "A distinction's committed capacity is recoverable by local operations iff no Moebius "
            "joint line containing it is billed at >= 2 interfaces. Forward = L_irr [P] (the check-4 "
            "simultaneity obstruction, reproduced in the line model on the bank's own tables). "
            "Backward = L_operational_completeness: footprint lines deletable by their owner (CM1 "
            "half), single-billed joint lines deletable by their sole owner (CM2 half), every "
            "intermediate admissible (L3). Footprint SPREAD across interfaces does not lock (the "
            "bank's s recoverable despite footprints at both); same-interface coupling does not lock "
            "(the 2026-06-09 audit countermodel, recoverable, floor 0 -- the retired arity criterion "
            "would have manufactured a massive record-free class). COROLLARY (ledger-level B3<- of "
            "the access-partition chain): the operational floor of a record class equals the "
            "locked-closure energy exactly (record support pinned, spectators stripped); a "
            "record-free class strips to the vacuum. Operational floor > 0 iff the class holds a "
            "record: mass is the locked closure, masslessness is operational (at the ledger "
            "level). GRADE [P] per the 2026-06-09 [P]-promotion audit: forward = L_irr [P]; "
            "backward = the clause [P] + L3; billing-locus = Paper 0 row 7b. The iff and the "
            "qualitative floor>0-iff-record corollary are fusion-fork-insensitive at every "
            "interface count; the quantitative closure identity is [P] on the record-support-"
            "closed class (the witnessed domain); the >= 3-interface fusion fork is DISSOLVED "
            "(2026-06-10, T_fusion_scope_dissolved: one semantics, two classes; E_rest(record) "
            "is the record-fact / billing-scope value). The spectrum export (ledger floor <-> "
            "Hamiltonian gap) stays behind "
            "OS2/ML + T_no_physical_time_flow_overclaim by design; no EW quantity moves."
        ),
        key_result=(
            "Recoverable iff Delta_cross = 0 (no joint line billed at >= 2 interfaces) -- [P], "
            "fork-insensitive. Record-class floor == locked-closure energy [P] on the support-"
            "closed class; record-free class floor = 0. The ledger-level converse (gapped <-> "
            "record) derived; X8 fork dissolved 2026-06-10 (T_fusion_scope_dissolved); "
            "spectrum export separately fenced."
        ),
        dependencies=["A1", "L_nc", "L_loc", "L_irr", "Paper0_row7b_defense_billing_locus"],
        cross_refs=["L_operational_completeness", "T_photon_massless_from_reversibility",
                    "T_ym_conformal_phase_excluded_by_record_locking",
                    "T_ledger_reversibility_is_L_irr_lock", "T_confinement"],
        artifacts={
            "criterion": "billing locus (joint line at >= 2 interfaces), NOT arity -- per cold audit",
            "floor_identity": "operational class floor == locked-closure energy (exact, witnessed)",
            "audit_countermodel": "same-interface coupling recoverable, floor 0 (T7)",
            "chain": "ledger-level B3<- of the access-partition arc; spectrum export fenced",
        },
    )




# ---------------------------------------------------------------------------
# The translation layer: P_Gamma-orbit operator model (refined product space)
# ---------------------------------------------------------------------------
class _RefinedWorld:
    """FD2/FD3-honest operator model: per-interface holding bits (d, g) for every
    nonzero footprint f_g(d); cross-billed joint lines are ONE fact (fused class --
    L_irr's simultaneity object); single-billed lines activate on their BILLING
    interface's bits (per-interface activation -- forced by FD1/FD3 + the W5
    false-lock countermodel; activation='fully_held' retained as the control).
    Full-scope MD guard (2026-06-09 translation-gate cold audit, correction 1):
    every member of every nonzero line carries a footprint at each billing
    interface."""

    def __init__(self, tables, activation="per_interface", guard=True):
        self.j = {g: _moebius(E, _U) for g, E in tables.items()}
        self.activation = activation
        self.bits = [(d, g) for d in sorted(_U) for g in sorted(self.j)
                     if self.j[g].get(frozenset({d}), 0) != 0]
        if guard:
            for g in self.j:
                for T, v in self.j[g].items():
                    if v != 0 and len(T) >= 2:
                        for d in T:
                            if self.j[g].get(frozenset({d}), 0) == 0:
                                raise ValueError(
                                    f"MD support violation: line {sorted(T)} billed "
                                    f"at {g} but member {d} has no footprint at {g}")
        parent = {b: b for b in self.bits}
        def find(x):
            while parent[x] != x:
                x = parent[x]
            return x
        def union(a, b):
            ra, rb = find(a), find(b)
            if ra != rb:
                parent[ra] = rb
        for g in self.j:
            for T, v in self.j[g].items():
                if v != 0 and len(T) >= 2 and len(self._owners(T)) >= 2:
                    tb = [bb for bb in self.bits if bb[0] in T]
                    for x in tb[1:]:
                        union(tb[0], x)
        cls = {}
        for b in self.bits:
            cls.setdefault(find(b), []).append(b)
        self.classes = list(cls.values())

    def _owners(self, T):
        return [g for g in self.j if self.j[g].get(T, 0) != 0]

    def _held_everywhere(self, state, d):
        db = [b for b in self.bits if b[0] == d]
        return bool(db) and all(state[b] for b in db)

    def _line_active(self, state, g, T):
        if self.activation == "fully_held" or len(self._owners(T)) >= 2:
            return all(self._held_everywhere(state, d) for d in T)
        return all(state.get((d, g), False) for d in T)

    def energy(self, g, state):
        e = sum(self.j[g].get(frozenset({d}), 0)
                for (d, gg) in self.bits if gg == g and state[(d, gg)])
        for T, v in self.j[g].items():
            if v != 0 and len(T) >= 2 and self._line_active(state, g, T):
                e += v
        return e

    def total(self, state):
        return sum(self.energy(g, state) for g in self.j)

    def admissible(self, state):
        return all(self.energy(g, state) <= _CAP for g in self.j)

    def flips(self, state, frozen_bits, removals_only=True):
        out = []
        for cl in self.classes:
            if any(b in frozen_bits for b in cl):
                continue
            val = state[cl[0]]
            if removals_only and not val:
                continue
            t = dict(state)
            for b in cl:
                t[b] = not val
            diffs = [g for g in self.j if self.energy(g, t) != self.energy(g, state)]
            if len(diffs) <= 1 and self.admissible(t):
                out.append((tuple(sorted(cl)), tuple(sorted(t.items()))))
        return out

    def reach(self, state, frozen_bits, removals_only=True):
        s0 = tuple(sorted(state.items()))
        seen, frontier = {s0}, [s0]
        while frontier:
            cur = dict(frontier.pop())
            for _, nxt in self.flips(cur, frozen_bits, removals_only):
                if nxt not in seen:
                    seen.add(nxt)
                    frontier.append(nxt)
        return seen

    def floor(self, record, removals_only=True):
        full = {b: True for b in self.bits}
        if not self.admissible(full):
            return None
        frozen = frozenset(b for b in self.bits if b[0] in record)
        return min(self.total(dict(st))
                   for st in self.reach(full, frozen, removals_only))

    def removable(self, d):
        db = [b for b in self.bits if b[0] == d]
        if not db:
            return None
        full = {b: True for b in self.bits}
        reach = self.reach(full, frozenset())
        return any(all(not dict(st)[b] for b in db) for st in reach)


def check_L_recoverability_is_orbit_reachability():
    """L_recoverability_is_orbit_reachability: cost-ledger recoverability and
    P_Gamma-orbit reachability compute the SAME functional -- the translation the
    Route C cold audit named as the chain's sharpest residue, decomposed, priced,
    and executed. Three steps are theorems (fields 3-5, 7-9); the two text
    consumptions are Paper 0 v6.2.27 rows 6/7a/7b (landed, 2026-06-09 principal
    ruling) + the full-scope MD support condition (field 6).

    Witness fields:
      1. W5 FALSE-LOCK CONTROL (the new countermodel, kept like V2a): two
         single-billed joints at different interfaces sharing member a, a
         footprinted at both. Fully-held activation imports an unenforced
         cross-interface binding at the PREDICATE level: record-class {b,c} orbit
         floor 10 vs locked closure 4 while Delta_cross(a) = 0 -- a false lock the
         banked field-3 logic contradicts. Invisible to the four prior worlds.
      2. PER-INTERFACE ACTIVATION (FD1/FD3-forced; Paper 0 row 7b): equivalence
         restored -- removable <=> Delta_cross = 0 on all five worlds; floor ==
         locked closure on W5 (4 and 2) and the mixed world.
      3. ACTION-LOCALITY THEOREM: every unfused bit-flip bills at its home
         interface ALONE (action locus == billing locus, derived from the model --
         non-vacuous: fully-held violates it on W5); every fused-class flip bills
         at >= 2 interfaces from every admissible state (falsifier (iii)'s object,
         in no P_Gamma).
      4. REMOVALS-ONLY WLOG: floors identical with re-addition flips enabled
         (monotone tables = L3's own premise; additions never unblock).
      5. CROSS-INTERFACE COMPOSITION: distinct-interface removal flips commute and
         stay available in either order -- the underspecified-by-design shared-
         configuration-space worry dissolves into the product structure (FD1/FD3).
      6. MD SUPPORT GUARD, FULL SCOPE -- DERIVED (FD3 + MD + row 7b, see GRADE):
         footprint-free members rejected in single- AND cross-billed lines (the
         translation-gate audit's central find: the single-billed-only guard
         admitted false-unlock worlds, bank 2 vs orbit 0).
      7. CAPSTONE: bank-clause reachable floors (the v24.3.230 subset semantics)
         == P_Gamma-orbit floors, every world x every record class.
      8. DIVERGENCE SCAN: under fully-held semantics the capstone diverges at
         exactly ONE cell (W5 x {b,c}) -- the repair is load-bearing and localized.
      9. EXHAUSTIVE SCAN: all 4096 two-interface worlds with footprints in {0,2}
         and pair-joints in {0,1}; on every MD-admitted world (324) the capstone
         holds for every record class -- 0 mismatches.

    GRADE [P] (2026-06-09 [P]-promotion audit): three translation steps are theorems
    (fields 3-5), certified exhaustively (field 9); the text consumptions are Paper 0
    v6.2.27 rows 6/7a/7b -- canonical, zero-translation, falsifier (iv); and the MD
    support condition is DERIVED, not a new identification: row 7b places every
    member of a nonzero Gamma-billed line at Gamma as a maintained distinction (FD3:
    distinctions are partitions of S_Gamma), MD prices each maintained
    Gamma-distinction at >= mu* > 0, and the standalone footprint line IS that cost
    by Moebius construction. Scope: two-interface worlds, |U| = 3 certification with
    size-free arguments (L_irr-parity evidentiary shape). SCOPE NOTE: the fusion-scope
    fork at >= 3 interfaces (does a record pin its members' foreign footprints?),
    named-not-decided at this check's landing, was DISSOLVED on 2026-06-10
    (T_fusion_scope_dissolved [P], promoted v24.3.234: one semantics, two pinned-fact classes;
    E_rest(record) is the record-fact / billing-scope value) -- this check remains
    restricted to two-interface worlds, where the two pin-sets coincide and the
    question never arises; the spectrum export stays behind OS2/ML +
    T_no_physical_time_flow_overclaim; no EW quantity moves. Reference:
    'Reference - The Translation Named - Recoverability and P_Gamma-Orbit
    Reachability (2026-06-09).md' (v0.2, audited REDUCES-WITH-CORRECTIONS, applied).
    Dependencies: A1, L_nc, L_loc, L_irr.
    """
    # local worlds (bank tables from module constants + the audit/mixed/W5 worlds)
    ES3 = {frozenset(): F(0), frozenset({0}): F(2), frozenset({1}): F(2), frozenset({2}): F(0),
           frozenset({0, 1}): F(5), frozenset({0, 2}): F(2), frozenset({1, 2}): F(2),
           frozenset({0, 1, 2}): F(5)}
    EE3 = {S: F(0) for S in ES3}
    ES2 = {frozenset(): F(0), frozenset({0}): F(3), frozenset({1}): F(2), frozenset({2}): F(2),
           frozenset({0, 1}): F(8), frozenset({0, 2}): F(5), frozenset({1, 2}): F(4),
           frozenset({0, 1, 2}): F(10)}
    EE2 = {frozenset(): F(0), frozenset({0}): F(2), frozenset({1}): F(3), frozenset({2}): F(1),
           frozenset({0, 1}): F(7), frozenset({0, 2}): F(3), frozenset({1, 2}): F(4),
           frozenset({0, 1, 2}): F(8)}
    ES5 = {frozenset(): F(0), frozenset({0}): F(2), frozenset({1}): F(2), frozenset({2}): F(0),
           frozenset({0, 1}): F(5), frozenset({0, 2}): F(2), frozenset({1, 2}): F(2),
           frozenset({0, 1, 2}): F(5)}
    EE5 = {frozenset(): F(0), frozenset({0}): F(2), frozenset({1}): F(0), frozenset({2}): F(2),
           frozenset({0, 1}): F(2), frozenset({0, 2}): F(5), frozenset({1, 2}): F(2),
           frozenset({0, 1, 2}): F(5)}
    worlds = {
        "coupled": {"S": _ES, "E": _EE},
        "additive": {"S": _ES_ADD, "E": _EE_ADD},
        "same_interface": {"S": ES3, "E": EE3},
        "mixed": {"S": ES2, "E": EE2},
        "W5": {"S": ES5, "E": EE5},
    }

    def bank_world(tables):
        return _LedgerWorld(tables, _CAP, _U)

    def bank_dc0(w, d):
        joints = {T for g in w.j for T, v in w.j[g].items()
                  if v != 0 and d in T and len(T) >= 2}
        return all(len(w.line_owners(T)) < 2 for T in joints)

    # 1. W5 false-lock control
    w5b = bank_world(worlds["W5"])
    _check(bank_dc0(w5b, 0), "field 1: Delta_cross(a) = 0 on W5 (both joints single-billed)")
    _check(w5b.removal_is_local(0, frozenset(_U)),
           "field 1: the bank clause says a is recoverable on W5")
    closure = w5b.total_energy(frozenset({1, 2}))
    rw5_fh = _RefinedWorld(worlds["W5"], activation="fully_held")
    _check(closure == F(4) and rw5_fh.floor(frozenset({1, 2})) == F(10),
           "field 1: fully-held activation manufactures a FALSE LOCK on W5 "
           "(orbit floor 10 vs locked closure 4) -- the unenforced binding at the "
           "predicate level, kept as control")

    # 2. per-interface activation restores the equivalence + floor identity
    for wname, tables in worlds.items():
        wb = bank_world(tables)
        rw = _RefinedWorld(tables)
        if not wb.admissible(frozenset(_U)):
            continue
        for d in sorted(_U):
            r = rw.removable(d)
            if r is None:
                continue
            _check(r == bank_dc0(wb, d),
                   f"field 2: removable <=> Delta_cross = 0 ({wname}, d={d})")
    rw5 = _RefinedWorld(worlds["W5"])
    _check(rw5.floor(frozenset({1, 2})) == closure == F(4),
           "field 2: W5 record {b,c} orbit floor == locked closure == 4")
    _check(rw5.floor(frozenset({1})) == w5b.total_energy(frozenset({1})) == F(2),
           "field 2: W5 record {b} orbit floor == locked closure == 2")
    wmb = bank_world(worlds["mixed"])
    rwm = _RefinedWorld(worlds["mixed"])
    _check(rwm.floor(frozenset({0, 1})) == wmb.total_energy(frozenset({0, 1})),
           "field 2: mixed-world record floor == locked closure (the banked field-5 "
           "identity, reproduced in the operator model)")

    # 3. action-locality theorem, every admissible reachable state, every world
    fused_seen = False
    for wname, tables in worlds.items():
        rw = _RefinedWorld(tables)
        full = {b: True for b in rw.bits}
        if not rw.admissible(full):
            continue
        seen = rw.reach(full, frozenset(), removals_only=False)
        for st in seen:
            state = dict(st)
            if not rw.admissible(state):
                continue
            for cl in rw.classes:
                t = dict(state)
                val = state[cl[0]]
                for b in cl:
                    t[b] = not val
                diffs = [g for g in rw.j if rw.energy(g, t) != rw.energy(g, state)]
                homes = {g for (_, g) in cl}
                if len(homes) == 1:
                    _check(set(diffs) <= homes,
                           f"field 3: unfused flip bills home-only ({wname})")
                else:
                    fused_seen = True
                    _check(len(diffs) >= 2,
                           f"field 3: fused-class flip bills at >= 2 interfaces ({wname})")
    _check(fused_seen, "field 3: fused classes exercised")

    # 4. removals-only WLOG
    for wname, tables in worlds.items():
        rw = _RefinedWorld(tables)
        if not rw.admissible({b: True for b in rw.bits}):
            continue
        for rec in _all_states(_U):
            _check(rw.floor(rec, removals_only=True) == rw.floor(rec, removals_only=False),
                   f"field 4: removals-only WLOG ({wname}, rec={sorted(rec)})")

    # 5. cross-interface commutation (availability order-stable, maps commute)
    import itertools as _it
    for wname, tables in worlds.items():
        rw = _RefinedWorld(tables)
        full = {b: True for b in rw.bits}
        if not rw.admissible(full):
            continue
        for st in rw.reach(full, frozenset()):
            state = dict(st)
            fl = rw.flips(state, frozenset())
            for (c1, t1), (c2, t2) in _it.combinations(fl, 2):
                h1 = {g for (_, g) in c1}
                h2 = {g for (_, g) in c2}
                if h1 & h2:
                    continue
                f1 = dict(rw.flips(dict(t1), frozenset()))
                f2 = dict(rw.flips(dict(t2), frozenset()))
                _check(c2 in f1 and c1 in f2,
                       f"field 5: availability order-stable ({wname})")
                _check(f1[c2] == f2[c1], f"field 5: orders agree ({wname})")

    # 6. MD support guard, full scope
    bad1 = {frozenset(): F(0), frozenset({0}): F(2), frozenset({1}): F(0), frozenset({2}): F(0),
            frozenset({0, 1}): F(5), frozenset({0, 2}): F(2), frozenset({1, 2}): F(0),
            frozenset({0, 1, 2}): F(5)}
    bad2 = {frozenset(): F(0), frozenset({0}): F(0), frozenset({1}): F(0), frozenset({2}): F(0),
            frozenset({0, 1}): F(0), frozenset({0, 2}): F(0), frozenset({1, 2}): F(1),
            frozenset({0, 1, 2}): F(1)}
    for bad, half in ((bad1, "single-billed"), (bad2, "cross-billed (the audit's false-unlock world)")):
        caught = False
        try:
            _RefinedWorld({"S": bad, "E": ({S: F(0) for S in bad} if half.startswith("single") else bad)})
        except ValueError:
            caught = True
        _check(caught, f"field 6: MD support violation rejected, {half} half")

    # 7. capstone on the five worlds
    for wname, tables in worlds.items():
        wb = bank_world(tables)
        rw = _RefinedWorld(tables)
        if not wb.admissible(frozenset(_U)):
            continue
        for rec in _all_states(_U):
            _check(wb.reachable_floor(frozenset(_U), rec) == rw.floor(rec),
                   f"field 7: bank floor == orbit floor ({wname}, rec={sorted(rec)})")

    # 8. divergence scan: fully-held breaks at exactly the W5 x {b,c} cell
    cells = []
    for wname, tables in worlds.items():
        wb = bank_world(tables)
        rwf = _RefinedWorld(tables, activation="fully_held")
        if not wb.admissible(frozenset(_U)):
            continue
        for rec in _all_states(_U):
            if wb.reachable_floor(frozenset(_U), rec) != rwf.floor(rec):
                cells.append((wname, tuple(sorted(rec))))
    _check(cells == [("W5", (1, 2))],
           "field 8: fully-held semantics diverges at exactly one cell (W5 x {b,c}) "
           "-- the per-interface repair is load-bearing and localized")

    # 9. exhaustive scan: every MD-admitted two-interface world, every record class
    fvals = (F(0), F(2))
    jvals = (F(0), F(1))
    pairs = (frozenset({0, 1}), frozenset({0, 2}), frozenset({1, 2}))
    def _table(fs, js):
        E = {frozenset(): F(0)}
        for S in _all_states(_U):
            if not S:
                continue
            e = sum(fs[d] for d in S)
            for p, jv in zip(pairs, js):
                if p <= S:
                    e += jv
            E[S] = e
        return E
    sides = [_table(dict(enumerate(fs)), js)
             for fs in _it.product(fvals, repeat=3)
             for js in _it.product(jvals, repeat=3)]
    admitted = mismatches = 0
    for tS in sides:
        for tE in sides:
            tables = {"S": tS, "E": tE}
            try:
                rw = _RefinedWorld(tables)
            except ValueError:
                continue
            wb = bank_world(tables)
            if not wb.admissible(frozenset(_U)):
                continue
            admitted += 1
            for rec in _all_states(_U):
                if wb.reachable_floor(frozenset(_U), rec) != rw.floor(rec):
                    mismatches += 1
    _check(admitted == 324 and mismatches == 0,
           f"field 9: exhaustive scan -- {admitted} MD-admitted worlds x 8 record "
           f"classes, {mismatches} mismatches (expected 324 and 0)")

    return _full_result(
        name="L_recoverability_is_orbit_reachability: ledger recoverability == P_Gamma-orbit reachability (the translation, named, executed, and paid)",
        tier=3,
        epistemic="P",
        summary=(
            "Cost-ledger recoverability (the v24.3.230 clause semantics) and P_Gamma-orbit "
            "reachability (compositions of interface-local admissible operators on the "
            "FD2/FD3-refined product space) compute the SAME floor functional: verified on the "
            "bank's worlds plus the W5 countermodel (field 7) and exhaustively across all 324 "
            "MD-admitted two-interface worlds x every record class (field 9, 0 mismatches). The "
            "operator model's activation predicate is forced: fully-held activation imports an "
            "unenforced cross-interface binding at the predicate level and manufactures a false "
            "lock on W5 (floor 10 vs closure 4, field 1) -- the V2a disease one level up -- and "
            "diverges from the bank at exactly that cell (field 8); per-interface activation "
            "(FD1/FD3; Paper 0 row 7b) restores equivalence and the floor=closure identity "
            "(field 2). Three translation steps are theorems: action-locality (unfused flips "
            "bill home-only, fused flips bill >= 2 -- falsifier (iii)'s object, field 3), "
            "removals-only WLOG (field 4), cross-interface composition via the product "
            "(field 5). The text consumptions are Paper 0 v6.2.27 sec:perturbations rows 6/7a/7b "
            "(landed by the 2026-06-09 principal ruling) + the full-scope MD support condition, "
            "DERIVED from FD3 + MD + row 7b (field 6 -- the translation-gate audit's central "
            "find). GRADE [P] per the 2026-06-09 [P]-promotion audit. SCOPE NOTE: the >= 3 "
            "interface fusion-scope fork, named at landing, was DISSOLVED 2026-06-10 "
            "(T_fusion_scope_dissolved); this check stays restricted to two-interface worlds, "
            "where the pin-sets coincide. NON-CLAIMS: the spectrum export stays behind OS2/ML + "
            "T_no_physical_time_flow_overclaim; no EW quantity moves."
        ),
        key_result=(
            "Ledger-clause reachable floors == P_Gamma-orbit floors: five worlds + 324-world "
            "exhaustive scan, every record class, 0 mismatches. Activation predicate forced by "
            "the W5 false-lock countermodel (10 vs 4); consumptions = Paper 0 rows 6/7a/7b + "
            "the DERIVED full-scope MD support condition. [P]"
        ),
        dependencies=["A1", "MD", "L_nc", "L_loc", "L_irr",
                      "Paper0_row6_operational_completeness",
                      "Paper0_row7a_threat_totality",
                      "Paper0_row7b_defense_billing_locus"],
        cross_refs=["L_operational_completeness", "L_local_removability",
                    "T_photon_massless_from_reversibility",
                    "T_ledger_reversibility_is_L_irr_lock"],
        artifacts={
            "translation": "5 steps: 3 theorems (fields 3-5) + rows 6/7a/7b (Paper 0 v6.2.27) + MD support condition",
            "countermodel": "W5 fully-held false lock 10 vs 4; divergence localized to one cell",
            "capstone": "324 MD-admitted worlds x 8 record classes, 0 mismatches",
            "fork_resolved": "fusion scope at >= 3 interfaces: DISSOLVED 2026-06-10 (T_fusion_scope_dissolved -- one semantics, two classes)",
            "reference": "Reference - The Translation Named - Recoverability and P_Gamma-Orbit Reachability (2026-06-09).md",
        },
    )

# ---------------------------------------------------------------------------
# The fusion-scope dissolution: fusion scope as an explicit parameter
# ---------------------------------------------------------------------------
class _ScopedWorld:
    """_RefinedWorld generalized: universe a parameter, per-interface activation,
    full-scope MD guard, and the cross-billed FUSION SCOPE explicit.
    scope='all_bits': a cross-billed line fuses ALL bits of its members (the
    v24.3.230 default; reproduces the subset model's freeze behavior -- field 2
    of the fourth check); scope='billing': it fuses its members' bits at the
    line's BILLING interfaces only (the record-FACT reading: Paper 0 row 7b's
    line-level instance)."""

    def __init__(self, tables, scope, universe):
        assert scope in ("all_bits", "billing")
        self.scope = scope
        self.universe = frozenset(universe)
        self.j = {g: _moebius(E, self.universe) for g, E in tables.items()}
        self.bits = [(d, g) for d in sorted(self.universe) for g in sorted(self.j)
                     if self.j[g].get(frozenset({d}), 0) != 0]
        for g in self.j:
            for T, v in self.j[g].items():
                if v != 0 and len(T) >= 2:
                    for d in T:
                        if self.j[g].get(frozenset({d}), 0) == 0:
                            raise ValueError("MD support violation")
        parent = {b: b for b in self.bits}
        def find(x):
            while parent[x] != x:
                x = parent[x]
            return x
        def union(a, b):
            ra, rb = find(a), find(b)
            if ra != rb:
                parent[ra] = rb
        for g in self.j:
            for T, v in self.j[g].items():
                if v != 0 and len(T) >= 2 and len(self._owners(T)) >= 2:
                    owners = set(self._owners(T))
                    if self.scope == "all_bits":
                        tb = [bb for bb in self.bits if bb[0] in T]
                    else:
                        tb = [bb for bb in self.bits
                              if bb[0] in T and bb[1] in owners]
                    for x in tb[1:]:
                        union(tb[0], x)
        cls = {}
        for b in self.bits:
            cls.setdefault(find(b), []).append(b)
        self.classes = list(cls.values())

    def _owners(self, T):
        return [g for g in self.j if self.j[g].get(T, 0) != 0]

    def _line_active(self, state, g, T):
        owners = self._owners(T)
        known = set(self.bits)
        if len(owners) >= 2:
            gs = self.j if self.scope == "all_bits" else owners
            return all(state.get((d, gg), True) for d in T for gg in gs
                       if (d, gg) in known)
        return all(state.get((d, g), False) for d in T)

    def energy(self, g, state):
        e = sum(self.j[g].get(frozenset({d}), 0)
                for (d, gg) in self.bits if gg == g and state[(d, gg)])
        for T, v in self.j[g].items():
            if v != 0 and len(T) >= 2 and self._line_active(state, g, T):
                e += v
        return e

    def total(self, state):
        return sum(self.energy(g, state) for g in self.j)

    def admissible(self, state):
        return all(self.energy(g, state) <= _CAP for g in self.j)

    def reach(self, frozen_bits):
        full = {b: True for b in self.bits}
        s0 = tuple(sorted(full.items()))
        seen, frontier = {s0}, [s0]
        while frontier:
            cur = dict(frontier.pop())
            for cl in self.classes:
                if any(b in frozen_bits for b in cl) or not cur[cl[0]]:
                    continue
                t = dict(cur)
                for b in cl:
                    t[b] = False
                diffs = [g for g in self.j
                         if self.energy(g, t) != self.energy(g, cur)]
                if len(diffs) <= 1 and self.admissible(t):
                    nxt = tuple(sorted(t.items()))
                    if nxt not in seen:
                        seen.add(nxt)
                        frontier.append(nxt)
        return seen

    def floor(self, frozen_bits):
        return min(self.total(dict(st)) for st in self.reach(frozen_bits))

    def per_interface_floors(self, frozen_bits):
        reach = [dict(st) for st in self.reach(frozen_bits)]
        mins = {g: min(self.energy(g, st) for st in reach) for g in self.j}
        attained = any(all(self.energy(g, st) == mins[g] for g in self.j)
                       for st in reach)
        return mins, attained

    def record_class(self, member_bit):
        for cl in self.classes:
            if member_bit in cl:
                return frozenset(cl)
        raise ValueError("bit not found")


def _wg_foreign_pins(w):
    """world-global foreign pins: every held cross-billed fact's members'
    single-billed foreign footprint bits (the 2026-06-10 audit's widening)."""
    pins = set()
    for g in w.j:
        for T, v in w.j[g].items():
            if v != 0 and len(T) >= 2 and len(w._owners(T)) >= 2:
                owners = set(w._owners(T))
                for d in T:
                    for (dd, gg) in w.bits:
                        if dd == d and gg not in owners:
                            pins.add((dd, gg))
    return frozenset(pins)


def check_T_fusion_scope_dissolved():
    """T_fusion_scope_dissolved: the X8 fusion-scope fork (Paper 3 supp red-team H5)
    is a NAME COLLISION, not an open decision -- one operator semantics, two
    pinned-fact classes.

    THE DISSOLUTION. The "all-bits arm" (the banked subset formula's default at
    >= 3 interfaces) and the "billing-scope arm" never disagreed about dynamics:
    they are the SAME per-interface-activation operator model handed two different
    pin-sets. The all-bits floor is the billing-scope floor of the class pinning
    the record AND -- world-globally, per the 2026-06-10 cold audit's correction --
    every held cross-billed fact's members' foreign holdings (field 4 executes the
    audit's |U| = 4 counterexample: the record-LOCAL form gives 16 against the
    all-bits 17; the |U| = 3 suite is structurally blind to it, since any second
    cross-billed line at three distinctions shares a member and welds in). Which
    pin-set deserves the name "the record class" is settled by canonical text:
    Paper 0 v6.2.27 row 7b's line-level instance (a Gamma-billed correlation is a
    correlation among the Gamma-aspects of its members -- verbatim) + FD1/FD3 (the
    only cross-interface objects in the bookkeeping are the multiply-billed
    correlations). The record class pins the record FACT; E_rest(record) is the
    billing-scope value; the all-bits value is the correct floor of the larger
    pinned class. Per-interface (where the first law lives), the closure identity
    is single-valued at EVERY interface count: at any interface the record does
    not bill, the record fact reads no bit and contributes zero to the floor
    (field 3) -- the fork's three-interface threshold was an artifact of the
    disjoint-cover model, and "uninvolved third interface" is cover-relative
    (interfaces overlap and nest, Paper 0 ~2234).

    Witness fields:
      1. ONE SEMANTICS, TWO CLASSES (canonical 3-interface probe): all-bits floor
         11 == billing floor with the foreign bit EXPLICITLY pinned; record-fact
         class floors at 10. The arms differ by pin-set, not dynamics.
      2. BANK == ALL-BITS at >= 3 interfaces: the subset model's reachable floor
         equals the all-bits class floor on the probe (11) and the |U| = 4 world
         (17) -- the banked formula was computing the larger class's floor.
      3. PER-INTERFACE FLOORS + SIMULTANEOUS ATTAINMENT: floor_S = 5, floor_E = 5,
         floor_G = 0 == the no-record G-floor; one reachable state attains every
         per-interface floor at once (monotone removals); cover-sum == sum of
         per-interface floors == 10.
      4. WORLD-GLOBAL IDENTITY (the audit's |U| = 4 spectator counterexample,
         executed): member-disjoint spectator record (c,d)@S,E with d
         foreign-footprinted at G -- all-bits 17; record-local augmented pins 16
         (the record-local identity FALSE); world-global pins 17 == all-bits ==
         bank.
      5. SUPPORT-CLOSURE SCAN (3 interfaces, deterministic family, 32 worlds):
         on every world-level support-closed world (8) billing == all-bits ==
         bank; on every open world (24) bank == all-bits == billing + the
         single-billed foreign footprints of held cross-billed facts' members --
         the excess is EXACTLY the unbilled pins, nothing else.
      6. UNWELDED CONTROL (4 interfaces, bit-disjoint under billing scope;
         member-shared, so all-bits welds -- the member-disjoint case is field 4):
         a second record sharing NO bit with the pinned one -- scopes agree anyway; L_irr's locality block on the second
         record's OWN class carries its commitments, weld or no weld.
      7. ZERO-LINES EXHIBIT: stripping the foreign footprint changes no S- and no
         E-energy and leaves the record line active -- the all-bits pin is
         supported by zero ledger lines at the record's own interfaces (the W5
         false-lock evidentiary shape).

    GRADE [P] (2026-06-10 dedicated fresh-context [P]-promotion audit:
    PROMOTE-WITH-CONDITIONS, conditions applied at v24.3.234; landed
    [P_structural] at v24.3.233 per the principal's two-step ruling; both audit
    verdicts appended to the reference note). The class-naming step (record
    class == record-fact pin-set) is consumed from row 7b's line-level instance
    PLUS the rows' landing paragraph -- Paper 0 v6.2.27 ~2981, named per the
    audit's condition 1: "the only cross-interface objects in the bookkeeping
    are the multiply-billed correlations" -- the sentence that closes "pinning
    the record pins nothing else" ANALYTICALLY, leaving the foreign pin no
    possible binder except the unenforced binding FD2/V2a convicts -- plus
    FD1/FD3. Fair-reading shape of check_L_epsilon_star; coin =
    interface-ontology row-consumption, inside the v24.3.232 license-class
    guard. LICENSE-CLASS GUARD (restated per condition 3): this promotion
    extends the interface-ontology row-consumption precedent and NOTHING else
    -- a "coupling = enforcement amplitude (w ~ g^2)" dictionary row landed
    today would NOT collect sin^2 theta_W = 3/13 at [P] tomorrow;
    physical-observable dictionary rows do not inherit, regardless of
    canonization. COMPOSED CONSEQUENCE (now carried, [P]): for every record and
    its record-fact class, at every interface count -- per-interface, the
    minimum of E_Gamma over the class equals the record's locked commitment in
    Gamma's own ledger (joint line + pinned support footprints at Gamma, clean
    spectators stripped); zero at every Gamma the record does not bill; one
    reachable state attains every per-interface floor simultaneously;
    E_rest(record) over any declared disjoint cover = the sum of per-interface
    floors. E_rest is a LEDGER quantity. NON-CLAIMS:
    the spectrum export stays behind OS2/ML + T_no_physical_time_flow_overclaim;
    the cover-sum is not deprecated (it is the sum of per-interface floors over a
    declared disjoint cover); the all-bits floor is not "wrong" (it answers a
    different question); no EW quantity moves. Reference: 'Reference - The
    Fusion-Scope Fork Dissolved - One Semantics, Two Classes (2026-06-10).md'
    (v0.2, cold-audit verdict operative).
    Dependencies: A1, MD, L_nc, L_loc, L_irr, rows 6/7a/7b.
    """
    U3 = frozenset({0, 1, 2})
    U4 = frozenset({0, 1, 2, 3})

    def _t(universe, fp, joints):
        E = {}
        for S in _all_states(universe):
            e = sum(fp.get(d, F(0)) for d in S)
            for T, v in joints.items():
                if T <= S:
                    e += v
            E[S] = e
        return E

    # canonical 3-interface probe (the X8 world of the 2026-06-09 witness)
    probe = {"S": _t(U3, {0: F(2), 1: F(1)}, {frozenset({0, 1}): F(2)}),
             "E": _t(U3, {0: F(1), 1: F(2)}, {frozenset({0, 1}): F(2)}),
             "G": _t(U3, {0: F(1)}, {})}

    # 1. one semantics, two classes
    ra = _ScopedWorld(probe, "all_bits", U3)
    rb = _ScopedWorld(probe, "billing", U3)
    rec_a = ra.record_class((0, "S"))
    rec_b = rb.record_class((0, "S"))
    _check((0, "G") in rec_a and (0, "G") not in rec_b,
           "field 1: the foreign bit is fused into the all-bits class only")
    fa, fb = ra.floor(rec_a), rb.floor(rec_b)
    fb_aug = rb.floor(rec_b | {(0, "G")})
    _check(fa == fb_aug == F(11) and fb == F(10),
           "field 1: all-bits floor == billing floor with the foreign bit "
           f"explicitly pinned ({fa} == {fb_aug}); record-fact class floor {fb} "
           "-- pin-set difference, not dynamics")

    # 2. bank == all-bits at >= 3 interfaces
    bank3 = _LedgerWorld(probe, _CAP, U3)
    _check(bank3.reachable_floor(frozenset(U3), frozenset({0, 1})) == fa == F(11),
           "field 2: the banked subset formula computes the all-bits class's floor "
           "at 3 interfaces (11)")

    # 3. per-interface floors + simultaneous attainment + cover-sum
    mins, attained = rb.per_interface_floors(rec_b)
    mins_free, _unused = rb.per_interface_floors(frozenset())
    _check(mins["S"] == F(5) and mins["E"] == F(5) and mins["G"] == F(0)
           and mins_free["G"] == F(0),
           "field 3: per-interface floors S=5, E=5, G=0 == no-record G-floor -- "
           "the record contributes zero to the uninvolved ledger")
    _check(attained and sum(mins.values()) == fb == F(10),
           "field 3: one reachable state attains every per-interface floor; "
           "cover-sum == sum of per-interface floors == 10")

    # 4. the audit's |U|=4 spectator counterexample: world-global identity
    spect = {"S": _t(U4, {0: F(2), 1: F(1), 2: F(1), 3: F(1)},
                     {frozenset({0, 1}): F(2), frozenset({2, 3}): F(1)}),
             "E": _t(U4, {0: F(1), 1: F(2), 2: F(1), 3: F(1)},
                     {frozenset({0, 1}): F(2), frozenset({2, 3}): F(1)}),
             "G": _t(U4, {3: F(1)}, {})}
    sa = _ScopedWorld(spect, "all_bits", U4)
    sb = _ScopedWorld(spect, "billing", U4)
    fa4 = sa.floor(sa.record_class((0, "S")))
    rec4 = sb.record_class((0, "S"))
    rl4 = sb.floor(rec4 | {(d, g) for (d, g) in sb.bits
                           if d in (0, 1) and g not in ("S", "E")})
    wg4 = sb.floor(rec4 | _wg_foreign_pins(sb))
    bank4 = _LedgerWorld(spect, _CAP, U4)
    bf4 = bank4.reachable_floor(frozenset(U4), frozenset({0, 1}))
    _check(fa4 == bf4 == F(17) and rl4 == F(16) and wg4 == F(17),
           f"field 4: |U|=4 spectator world -- all-bits {fa4} == bank {bf4}; "
           f"record-local pins {rl4} (the record-local identity FALSE); "
           f"world-global pins {wg4} == all-bits (the identity is true on the "
           "WORLD, not the record)")

    # 5. world-level support-closure scan
    closed = opened = bad = 0
    for aG in (F(0), F(1)):
        for bG in (F(0), F(1)):
            for cS in (F(0), F(1)):
                for cE in (F(0), F(1)):
                    for cG in (F(0), F(1)):
                        w = {"S": _t(U3, {0: F(1), 1: F(1), 2: cS},
                                     {frozenset({0, 1}): F(1)}),
                             "E": _t(U3, {0: F(1), 1: F(1), 2: cE},
                                     {frozenset({0, 1}): F(1)}),
                             "G": _t(U3, {0: aG, 1: bG, 2: cG}, {})}
                        bank = _LedgerWorld(w, _CAP, U3)
                        wa = _ScopedWorld(w, "all_bits", U3)
                        wb = _ScopedWorld(w, "billing", U3)
                        bf = bank.reachable_floor(frozenset(U3), frozenset({0, 1}))
                        fa_ = wa.floor(wa.record_class((0, "S")))
                        fb_ = wb.floor(wb.record_class((0, "S")))
                        foreign = aG + bG
                        if foreign == 0:
                            closed += 1
                            if not (bf == fa_ == fb_):
                                bad += 1
                        else:
                            opened += 1
                            if not (bf == fa_ == fb_ + foreign):
                                bad += 1
    _check(closed == 8 and opened == 24 and bad == 0,
           f"field 5: support-closure scan -- {closed} closed worlds agree three "
           f"ways; {opened} open worlds: bank == all-bits == billing + foreign "
           f"footprints exactly ({bad} violations)")

    # 6. unwelded control (4 interfaces)
    ctl = {"S": _t(U3, {0: F(2), 1: F(1)}, {frozenset({0, 1}): F(2)}),
           "E": _t(U3, {0: F(1), 1: F(2)}, {frozenset({0, 1}): F(2)}),
           "G": _t(U3, {0: F(1), 2: F(1)}, {frozenset({0, 2}): F(1)}),
           "H": _t(U3, {0: F(1), 2: F(1)}, {frozenset({0, 2}): F(1)})}
    ca = _ScopedWorld(ctl, "all_bits", U3)
    cb = _ScopedWorld(ctl, "billing", U3)
    _check((0, "G") not in cb.record_class((0, "S")),
           "field 6: billing scope -- the second record's class shares no bit "
           "with the pinned one (unwelded)")
    _check(ca.floor(ca.record_class((0, "S"))) == cb.floor(cb.record_class((0, "S"))),
           "field 6: floors agree anyway -- L_irr's locality block on the second "
           "record's OWN class carries its commitments; the weld is incidental")

    # 7. zero-lines exhibit
    full = {b: True for b in rb.bits}
    stripped = dict(full)
    stripped[(0, "G")] = False
    _check(rb.energy("S", full) == rb.energy("S", stripped)
           and rb.energy("E", full) == rb.energy("E", stripped),
           "field 7: stripping the foreign footprint changes no record-interface "
           "energy")
    _check(rb._line_active(stripped, "S", frozenset({0, 1}))
           and rb._line_active(stripped, "E", frozenset({0, 1}))
           and rb.admissible(stripped),
           "field 7: the record fact stays held; the all-bits pin is supported by "
           "zero ledger lines at the record's interfaces")

    return _full_result(
        name="T_fusion_scope_dissolved: the X8 fork is one semantics on two pinned-fact classes; E_rest(record) is the record-fact (billing-scope) value",
        tier=3,
        epistemic="P",
        summary=(
            "The fusion-scope fork (Paper 3 supp red-team H5; the named non-claim of the "
            "v24.3.231/232 translation landing) dissolves: the all-bits and billing-scope "
            "arms are ONE per-interface-activation operator semantics handed two different "
            "pin-sets. All-bits floor == billing-scope floor of the class pinning the record "
            "and (world-globally -- the 2026-06-10 audit's correction, record-local form "
            "FALSE at |U|=4: 17 vs 16) every held cross-billed fact's members' foreign "
            "holdings; exhibited directly (foreign bit explicitly pinned reproduces 11) and "
            "the banked subset formula shown to compute the all-bits class's floor at >= 3 "
            "interfaces. The record class pins the record FACT -- row 7b verbatim (a "
            "Gamma-billed correlation is a correlation among the Gamma-aspects of its "
            "members) + FD1/FD3 (the only cross-interface objects in the bookkeeping are "
            "the multiply-billed correlations) -- so E_rest(record) is the billing-scope "
            "value, and per-interface (where the first law lives) the closure identity is "
            "single-valued at every interface count: the record contributes zero to any "
            "ledger it does not bill; one reachable state attains every per-interface floor "
            "at once; the cover-sum is their sum; the all-bits excess is exactly the "
            "single-billed foreign footprints (32-world scan, 0 violations); a cross-billed "
            "foreign commitment is carried by L_irr's own lock under either scope (unwelded "
            "4-interface control). Third instance of one disease, same cure: V2a (state "
            "space), W5 (activation predicate), X8 (class definition). GRADE [P] per the "
            "2026-06-10 dedicated fresh-context promotion audit (PROMOTE-WITH-CONDITIONS, "
            "applied at v24.3.234): the class-naming closes analytically via row 7b + "
            "Paper 0 ~2981 (the only cross-interface objects are the multiply-billed "
            "correlations); interface-ontology coin; license-class guard restated -- "
            "dictionary rows do not inherit, sin^2 theta_W stays [P_structural]. The "
            "spectrum export stays fenced; the "
            "cover-sum stays a legitimate declared-cover tool; no EW quantity moves."
        ),
        key_result=(
            "X8 fork dissolved: one semantics, two classes (11 == 11 with the pin made "
            "explicit; record-fact floor 10). Record class pins the record fact (row 7b); "
            "E_rest(record) = billing-scope value; per-interface closure identity "
            "single-valued at every interface count; all-bits = floor of the larger "
            "world-global pinned class (|U|=4: 17/16/17). [P]"
        ),
        dependencies=["A1", "MD", "L_nc", "L_loc", "L_irr",
                      "L_local_removability",
                      "L_recoverability_is_orbit_reachability",
                      "Paper0_row6_operational_completeness",
                      "Paper0_row7a_threat_totality",
                      "Paper0_row7b_defense_billing_locus"],
        cross_refs=["L_operational_completeness", "L_local_removability",
                    "L_recoverability_is_orbit_reachability",
                    "T_ledger_reversibility_is_L_irr_lock",
                    "T_photon_massless_from_reversibility"],
        artifacts={
            "dissolution": "one semantics, two pinned-fact classes; the arms differ by pin-set",
            "world_global": "all-bits == billing + world-global foreign pins (record-local FALSE at |U|=4: 17 vs 16)",
            "per_interface": "closure identity per-interface, single-valued at every interface count; cover-sum = sum of floors",
            "bank_default": "the banked subset formula computes the all-bits (larger) class's floor at >= 3 interfaces",
            "reference": "Reference - The Fusion-Scope Fork Dissolved - One Semantics, Two Classes (2026-06-10).md",
        },
    )


def check_T_ledger_rent_excluded():
    """T_ledger_rent_excluded: the ledger admits exactly two kinds of bookable
    cost (no standing rent) -- the cost-kind dichotomy, Paper 0 row 9. [P]

    STATEMENT. Every cost the ledger books is one of exactly two kinds:
      (a) a formation/realignment commitment, booked at a transition and
          standing thereafter as committed capacity (locked iff it participates
          in a joint line billed at >= 2 interfaces, L_local_removability);
      (b) a per-activation charge, booked when and only when an interface
          activates on held content (the per-interface activation semantics of
          T_fusion_scope_dissolved).
    These are one transition-booked semantics distinguished by which transition
    toggles the booking. The excluded third kind is exactly a booking change
    with no transition of any type: a standing-rent term -- a cost of merely
    persisting, accruing under slicing at fixed structure -- is incoherent in
    the ledger as constituted.

    PROOF CHAIN (load path per the 2026-06-11 sealed-cycle cold audit):
      1. A1's bound is a LEVEL: sum_d eps(d) <= C(rho, Gamma), with eps a
         function of the distinction (and interface) -- no time argument exists
         in eps's domain for a rent to accrue against (Paper 0 sec:A1; the cost
         spectrum is read off admissibility-space structure, not off any
         external clock or thermal bath).
      2. The eternalist commitment (Paper 0 sec:descriptive_temporal): the
         accrual reading of holding cost is canonically a slicing artifact;
         time is derived (Paper 3), not primitive -- a rent rate would smuggle
         the derived quantity into a primitive.
      3. Accumulated cost is a STATE FUNCTION: E(state) = n*eps,
         path-independent (Paper 3 Supplement, access partition; the 2026-06-09
         Reading-A ruling recorded in this module's header). A rent term makes
         the booked value history-dependent at fixed state -- contradiction.
      4. T_realignment_cost_is_transition_energy [P]: C(transition) = n*eps =
         DeltaE. A rent tick is either a null transition booking r > 0
         (contradicts the identity) or no transition at all (contradicts 3).
      5. Under rent, a fixed admissible configuration crosses the A1 bound in
         finitely many slicing ticks with NO transition -- the admissible set,
         which every downstream theorem quantifies over, ceases to be well
         defined (witnessed exactly, field 3 below).
      6. L_cost [P] contributes uniqueness of the realignment functional;
         T_no_physical_time_flow_overclaim corroborates (the one dynamics-like
         calculus in the bank explicitly declines a physical-time flow and
         names the continuation-flow law a future program).

    SEMANTICS (row 9's clause). "Maintenance cost" / "maintenance demand"
    names the standing committed-capacity LEVEL a holding places against
    C(rho, Gamma) -- a deposit held, booked at formation, not a payment
    stream. The keystone's "the commitment is continuous" reads: continuous
    as commitment, not as payment. Conservative over every banked use
    (2026-06-11 sweeps, papers + bank: no contradiction found; the demand
    column unchanged -- theta 0 / condensate 1 / colour 0; the corpus's
    "hold available" vocabulary is this theorem in prose form).

    GRADE [P]: Paper 0 v6.2.29 sec:perturbations row 9 (cost-kind
    dichotomy, no standing rent) -- a canonical published definitional
    commitment consumed at zero translation, with falsifier (vi) exposure --
    over A1 + the eternalist commitment + the Paper 3 state-function sentence
    + T_realignment_cost_is_transition_energy [P] + L_cost [P]. Precedent
    class: check_L_operational_completeness [P] over row 6;
    check_L_epsilon_star [P] over MD/BW. The pre-row sealed-cycle derivation
    carried the dual-reading consumption as a named prose-level
    identification; row 9 lifts it to row parity (the dual-reading paragraph
    is the row's named semantics clause).

    LICENSE-CLASS GUARD (unchanged): interface-ontology rows only;
    physical-dictionary rows do not inherit; sin^2 theta_W stays
    [P_structural]. The theorem is PLACEMENT-BLIND -- it fixes WHEN a
    commitment books, never WHERE among sector rows (2026-06-11 step-3 cycle,
    W1 placement-blindness) -- and licenses no billing-placement claim; UB
    stays adopted-with-falsifier.

    SCOPE: the exclusion holds for the ledger as constituted; a future banked
    continuation-flow law would reopen the question for that extended ledger.
    FALSIFIER (vi): a demonstrated booking change with no transition of any
    type (no realignment, no interface activation).

    WITNESS-TAG DISCIPLINE: fields below are labeled per the 2026-06-11 audit
    convention -- exact instantiations of the exclusion's arithmetic on the
    bank's own tables (illustrative); the discriminating content lives in the
    row + the cited canonical text. Refs: 'Reference - The Rent Question -
    Sealed Cycle Verdict (2026-06-11)' v0.2 + its cold audit + 'Reference -
    The Rent Exclusion - Ontological Absorption Map (2026-06-11)'; standalone
    witness rent_question_witness.py at APF Reference Docs.
    """
    rate = F(1, 4)                  # any positive rent rate (value immaterial)

    # field 1: booking is keyed by state alone on the bank's own tables; the
    # rent rival departs from the state-function value at the first tick
    for E in (_ES, _EE):
        for S, v in E.items():
            _check(E[S] == v, "field 1: booking keyed by state alone (table fact)")
            _check(v + rate != v,
                   "field 1 (exact instantiation): the rent-extended booking "
                   "E + r*t departs from the banked state-function value at "
                   "t = 1; two histories reaching the same state disagree "
                   "under rent and agree under the banked booking")

    # field 2: the null transition books zero; the rent tick's disjunction
    eps = F(1)
    n_realigned = 0
    _check(n_realigned * eps == 0 and rate != 0,
           "field 2 (the step-4 disjunction, instantiated): C(null transition) "
           "= n*eps = 0 = DeltaE (T_realignment_cost_is_transition_energy), "
           "while a rent tick books DeltaE = r > 0 -- as a transition it "
           "contradicts the identity; as a non-transition it contradicts the "
           "state function (field 1)")

    # field 3: under rent a fixed admissible configuration crosses the bound
    # in an exactly computed finite tick count with zero transitions
    w = _LedgerWorld({"S": _ES, "E": _EE}, _CAP, _U)
    adm = [S for S in _all_states(_U) if w.admissible(S)]
    _check(len(adm) > 0, "field 3: the coupled world has admissible states")
    S_star = max(adm, key=lambda S: w.total_energy(S))
    E0 = w.total_energy(S_star)
    cap_sum = len(w.j) * _CAP
    _check(E0 <= cap_sum, "field 3: S_star is admissible at t = 0 (level within the bound)")
    ticks = int((cap_sum - E0) / rate) + 1
    _check(E0 + rate * ticks > cap_sum,
           f"field 3: under rent the SAME configuration crosses the bound after "
           f"exactly {ticks} ticks with zero transitions -- the admissible set "
           "is no longer well defined (A1's bound is a level; the sealed "
           "verdict's step-6 contradiction, on the bank's own tables)")

    # field 4 (ILLUSTRATIVE-OF-TEXT): every booking event in the bank's own
    # world coincides with a state transition; non-vacuity is the real assert
    events = 0
    for S0 in _all_states(_U):
        if not w.admissible(S0):
            continue
        for d in set(S0):
            if w.removal_is_local(d, S0):
                S1 = S0 - {d}
                events += 1
                _check(S1 != S0 and w.total_energy(S1) == w.total_energy(S1),
                       "field 4 ILLUSTRATIVE-OF-TEXT: booking changes ride "
                       "state transitions on the bank's own tables (the model "
                       "books by state; the textual carriers are row 9 + the "
                       "eternalist commitment)")
    _check(events > 0, "field 4: the booking-event scan is non-vacuous")

    # field 5: falsifier (vi) is well-posed and live
    def _falsifier_vi(delta_booking, n_realigned_, activation_toggles):
        return delta_booking != 0 and n_realigned_ == 0 and activation_toggles == 0
    _check(not _falsifier_vi(F(0), 0, 0),
           "field 5: the null event does not fire falsifier (vi)")
    _check(not _falsifier_vi(F(3), 3, 0) and not _falsifier_vi(F(1), 0, 1),
           "field 5: kind-(a) and kind-(b) bookings do not fire falsifier (vi) "
           "(each rides a transition of its own type)")
    _check(_falsifier_vi(rate, 0, 0),
           "field 5: the rent tick has EXACTLY falsifier (vi)'s shape -- a "
           "booking change with no transition of any type; the falsifier is "
           "live and well-posed, not decorative")

    return _full_result(
        name="T_ledger_rent_excluded: the ledger admits exactly two cost kinds -- formation/realignment commitments + per-activation charges; no standing rent (Paper 0 row 9)",
        tier=3,
        epistemic="P",
        summary=(
            "Every booked cost is a transition-booked level: a formation/realignment commitment "
            "standing thereafter as committed capacity (locked iff in a multiply-billed joint line), "
            "or a per-activation charge. No third kind: a standing-rent term (cost of merely "
            "persisting, accruing at fixed structure) is excluded by A1's level-form bound (no time "
            "argument in eps's domain), the eternalist commitment (accrual = slicing artifact; time "
            "derived not primitive), the Paper 3 state-function property E(state) = n*eps, and "
            "T_realignment_cost_is_transition_energy (the null transition books zero). 'Maintenance "
            "cost' = the standing committed level, not a payment stream; conservative over every "
            "banked use (2026-06-11 corpus sweeps; demand column theta 0 / condensate 1 / colour 0 "
            "unchanged). GRADE [P] over Paper 0 v6.2.29 row 9 (zero-translation row consumption, "
            "falsifier (vi)), precedent class row-6/MD-BW. License-class guard: interface-ontology "
            "rows only; the theorem is placement-blind (fixes WHEN booking occurs, never WHERE among "
            "sector rows) and licenses no billing-placement claim; sin^2 theta_W stays [P_structural]; "
            "UB stays adopted-with-falsifier. Scope: the ledger as constituted (a future banked "
            "continuation-flow law would reopen it)."
        ),
        key_result=(
            "Two cost kinds, no standing rent: nothing pays rent -- everything pays deposits "
            "(formation commitments standing as committed capacity) and reading fees "
            "(per-activation charges). The record ontology's completeness clause. [P]"
        ),
        dependencies=["A1", "L_cost", "L_irr", "L_loc",
                      "T_realignment_cost_is_transition_energy",
                      "L_local_removability",
                      "Paper0_row9_cost_kind_dichotomy"],
        cross_refs=["T_fusion_scope_dissolved", "L_operational_completeness",
                    "T_thermodynamics_four_laws_synthesis",
                    "T_photon_massless_from_reversibility",
                    "T_particle_mass_is_locked_record", "T_theta_QCD",
                    "T_no_physical_time_flow_overclaim", "L_epsilon_star"],
        artifacts={
            "dichotomy": "kind (a) formation commitment (deposit/level) + kind (b) per-activation charge; one transition-booked semantics",
            "excluded": "booking change with no transition of any type (= falsifier (vi)'s exact shape)",
            "tick_witness": "rent crosses the bound in an exactly computed finite tick count with zero transitions",
            "semantics": "maintenance cost = standing committed level (deposit held, not rent paid); keystone 'continuous' = continuous as commitment",
            "placement_blindness": "fixes WHEN a commitment books, never WHERE among sector rows (step-3 W1); no billing-placement license",
            "provenance": "sealed cycle 2026-06-11 (target-blind) -> cold audit LAND-WITH-CORRECTIONS -> row 9 landed -> this consumption",
        },
    )


def check_T_transition_log_residue_readable():
    """T_transition_log_residue_readable: the ledger's transition log is
    observable at exactly residue grade -- the pin-indexed booked configuration
    plus its locked closure -- under Paper 0 row 10 (observer-reading). [P]

    STATEMENT. The ledger basis grants observable status to the state-borne
    residue of its own transition log:
      (1) every commitment booking is transition-toggled and materialized as a
          standing-level change -- the booking IS the event's residue (row 9,
          T_ledger_rent_excluded [P]);
      (2) the residue is pin-indexed by construction -- the configuration
          space is the product of per-interface holding bits (d, Gamma), so
          WHICH distinction stands is the state, not a meta-fact about it
          (refined product space, L_recoverability_is_orbit_reachability [P]);
      (3) the residue is evaluable at its billing interfaces: row 10
          (observer-reading, Paper 0 v6.2.30) -- every fact constituted wholly
          by FD1-triple content held at Gamma is evaluable at Gamma; an
          enacted operation's outcome is the post-state, itself held content;
          row 8 types the evaluation (interface-internal), row 6 +
          L_local_removability supply the per-distinction operator inventory
          the reading turns into probes;
      (4) the locked part of the residue is unerasable by any local operation
          (L_irr [P]) -- the readable log cannot be silently rewritten.

    FENCES (the grant's exact extent, all asserted below): reading caps at
    state residue -- two histories composing to the same configuration are
    log-indistinguishable (row 9's state-function discipline, the canonical
    name for the basis's silence); activation-only (kind (b)) events leave no
    standing level and sit outside the residue-readable log; counterfactual
    (unenacted) profiles are not granted; the unit-level holding assignment h
    is NOT residue -- placements are separated only THROUGH enacted
    transitions' differential pin-death, never by inspecting h.

    CONSEQUENCE (the two-placement fork's YES branch, executed in field 3):
    under the same pool-addressed withdrawal, ALIGNED and BORROWED placements
    leave different pin-indexed residues -- the first enacted withdrawal's
    record separates what no L-definable observable could (the 2026-06-12
    two-placement enrichment's under-determination) -- while anonymized
    (pin-name-stripped) survivor counts coincide across classes under the
    mirror pairing: the separation is carried by pin identity exactly as the
    M1 retype said. JOIN SPLIT (the adjudication audit's M2): the row grain
    pin x pool takes its PIN coordinate from this theorem and its POOL
    coordinate from the enactment (the experiment's own address) -- no
    canonical row delivers the pool coordinate, and this check licenses no
    billing-placement claim.

    GRADE [P]: Paper 0 v6.2.30 sec:perturbations row 10 (observer-reading: reading
    totality over held content, the converse completion of storage-totality)
    consumed at zero translation, with falsifier (vii) exposure -- over
    T_ledger_rent_excluded [P] + L_recoverability_is_orbit_reachability [P] +
    L_local_removability [P] + L_irr [P]. Precedent class:
    check_T_ledger_rent_excluded [P] over row 9; check_L_operational_completeness
    [P] over row 6. Landed row-first per the principal's 2026-06-12 ruling
    (option 2 of the L2-observability adjudication): the row is the naming.

    LICENSE-CLASS GUARD: interface-ontology rows only; physical-dictionary
    rows do not inherit; sin^2 theta_W stays [P_structural]. The theorem is
    billing-placement-blind: it grants reading of residue, never placement of
    bills among sector rows; UB stays adopted-with-falsifier; the
    threat-localization readings stay unadopted.

    FALSIFIERS: (vii) a maintained interface with a fact constituted wholly by
    its own held FD1-triple content that is demonstrably unevaluable at that
    interface (kills row 10, and with it this consumption); a verified
    configuration whose billed quantities differ while its full pin-indexed
    residue is identical under every enacted transition (billing reading
    something residue-invisible breaks step 3); row 9's falsifier (vi)
    inherited (a booking change with no transition).

    PROVENANCE: 'Reference - The L2-Observability Adjudication (2026-06-12).md'
    v0.2 (canonical pass; cold audit REDUCES 1H+3M+5m, corrections in-body;
    OR named) -> principal ruling 2026-06-12 (OR landed as Paper 0 row 10,
    v6.2.30) -> this consumption. Upstream: the two-placement enrichment +
    enforcement-reading executable cycles (both 2026-06-12, sealed, audited).

    WITNESS-TAG DISCIPLINE: fields 1-2 are exact instantiations on the bank's
    own tables (illustrative of the canonical clauses); field 3 is the
    condensed two-placement model from two_placement_enrichment_witness.py;
    the discriminating content lives in row 10 + the cited canonical text.
    """
    # field 1 (row 9, residue materialization): on the bank's own coupled
    # world, every available (local) removal is a transition whose residue is
    # a strict standing-level change at the billing interface
    w = _LedgerWorld({"S": _ES, "E": _EE}, _CAP, _U)
    moves = 0
    for S0 in _all_states(_U):
        if not w.admissible(S0):
            continue
        for d in set(S0):
            if w.removal_is_local(d, S0):
                S1 = S0 - {d}
                moves += 1
                _check(S1 != S0, "field 1: the enacted removal leaves a configuration residue")
                _check(w.total_energy(S1) < w.total_energy(S0),
                       "field 1: the unbooking is materialized as a strict "
                       "standing-level change (MD support: footprints positive)")
    _check(moves > 0, "field 1: the removal scan is non-vacuous")

    # field 2 (refined product space, pin-indexing): the residue identifies
    # WHICH distinction died -- distinct removals leave distinct configurations
    S_full = frozenset(_U)
    posts = {d: S_full - {d} for d in _U}
    for d1 in _U:
        for d2 in _U:
            if d1 != d2:
                _check(posts[d1] != posts[d2],
                       "field 2: pin-indexed residue -- the post-configuration "
                       "separates which distinction was unbooked (the state IS "
                       "the booked-bit product, no anonymized level beneath)")

    # field 3 (row 10 consumed; the two-placement separation): condensed
    # arena from the 2026-06-12 two-placement enrichment witness
    POOL_OF = {"a1": "A", "a2": "A", "b1": "B", "b2": "B"}
    POOL_UNITS = {"A": ("a1", "a2"), "B": ("b1", "b2")}
    PINS = ("p1", "p2", "p3")
    PLACEMENTS = {
        "ALIGNED/p3@A":  {"p1": "a1", "p2": "b1", "p3": "a2"},
        "ALIGNED/p3@B":  {"p1": "a1", "p2": "b1", "p3": "b2"},
        "BORROWED/p1>B": {"p1": "b1", "p2": "b2", "p3": "a1"},
        "BORROWED/p2>A": {"p1": "a1", "p2": "a2", "p3": "b1"},
    }

    def withdraw(h, units_gone):
        # enact withdrawals in sequence; same-pool re-assignment is a booked
        # transition; a pin with no free same-pool unit dies (is unbooked)
        h = dict(h)
        gone = set()
        for u in units_gone:
            gone.add(u)
            victim = next((p for p in PINS if h.get(p) == u), None)
            if victim is not None:
                pool = POOL_OF[u]
                free = [x for x in POOL_UNITS[pool]
                        if x not in gone and x not in h.values()]
                if free:
                    h[victim] = free[0]      # booked re-assignment transition
                else:
                    del h[victim]            # the pin dies -- unbooked
        return frozenset(h.keys())           # the pin-indexed residue

    # (3a) the same pool-addressed enactment leaves class-separating residue
    res_aligned = withdraw(PLACEMENTS["ALIGNED/p3@A"], ("b1",))
    res_borrow = withdraw(PLACEMENTS["BORROWED/p1>B"], ("b1",))
    _check(res_aligned == frozenset({"p1", "p2", "p3"}),
           "field 3a: ALIGNED survives the pool-B withdrawal intact "
           "(same-pool re-assignment, a booked transition)")
    _check(res_borrow == frozenset({"p2", "p3"}),
           "field 3a: under BORROWED the same enactment kills p1 -- an "
           "A-constituted pin dead by a B-pool withdrawal, the borrow exposed")
    _check(res_aligned != res_borrow,
           "field 3a: the first enacted withdrawal's residue separates the "
           "placements -- readable under row 10, pin-indexed per field 2")

    # (3b) pin-identified withdrawal profiles are class-disjoint while
    # anonymized survivor counts coincide under the mirror pairing -- the
    # separation is carried by pin identity (M1), not by counts
    def profile(name):
        return tuple(sorted((u, tuple(sorted(withdraw(PLACEMENTS[name], (u,)))))
                            for u in ("a1", "a2", "b1", "b2")))

    def counts(name):
        return tuple(sorted((u, len(withdraw(PLACEMENTS[name], (u,))))
                            for u in ("a1", "a2", "b1", "b2")))

    profs = {n: profile(n) for n in PLACEMENTS}
    for nA in ("ALIGNED/p3@A", "ALIGNED/p3@B"):
        for nB in ("BORROWED/p1>B", "BORROWED/p2>A"):
            _check(profs[nA] != profs[nB],
                   "field 3b: pin-identified profiles are class-disjoint")
    # mirror pairing: pool-relabel A<->B (a_i <-> b_i) maps the count profile
    swap = {"a1": "b1", "a2": "b2", "b1": "a1", "b2": "a2"}
    def counts_swapped(name):
        return tuple(sorted((swap[u], c) for (u, c) in counts(name)))
    _check(counts_swapped("ALIGNED/p3@A") == counts("BORROWED/p2>A") or
           counts("ALIGNED/p3@A") == counts("BORROWED/p2>A"),
           "field 3b: anonymized survivor counts coincide across classes under "
           "the mirror pairing -- bare counts do not separate (the witness's "
           "masking fact, condensed)")

    # field 4 (the fences): (i) residue cap -- two enactment orders composing
    # to the same withdrawn set leave the same residue (state function; the
    # sequence-level log is NOT granted)
    for name in PLACEMENTS:
        _check(withdraw(PLACEMENTS[name], ("b1", "b2")) ==
               withdraw(PLACEMENTS[name], ("b2", "b1")),
               "field 4i: order-composed histories are log-indistinguishable "
               "at fixed residue (row 9 state-function cap)")
    # (ii) h is not residue: pre-enactment, every placement holds the same
    # pin-indexed configuration -- the reading right adds nothing unenacted
    pre = {n: frozenset(PLACEMENTS[n].keys()) for n in PLACEMENTS}
    _check(len(set(pre.values())) == 1,
           "field 4ii: pre-enactment residues are placement-identical -- the "
           "holding assignment h is invisible to the granted reading; "
           "separation exists only THROUGH enacted transitions")
    # (iii) activation-only events: a kind-(b) charge with zero commitment
    # change leaves the residue untouched -- outside the readable log
    _check(withdraw(PLACEMENTS["ALIGNED/p3@A"], ()) ==
           frozenset({"p1", "p2", "p3"}),
           "field 4iii: the null/activation-only event leaves no residue -- "
           "the readable log carries commitment bookings only")

    # field 5: falsifier (vii) is well-posed and live
    def _falsifier_vii(held_wholly_constituted, evaluable_at_interface):
        return held_wholly_constituted and not evaluable_at_interface
    _check(not _falsifier_vii(True, True),
           "field 5: row 10's grant does not fire falsifier (vii)")
    _check(not _falsifier_vii(False, False),
           "field 5: a fact not wholly held at Gamma is outside the row's "
           "scope (row 8 contrapositive) and does not fire (vii)")
    _check(_falsifier_vii(True, False),
           "field 5: a held-but-unevaluable fact has EXACTLY falsifier "
           "(vii)'s shape -- the falsifier is live and well-posed")

    return _full_result(
        name="T_transition_log_residue_readable: the ledger's transition log is observable at residue grade -- pin-indexed booked configuration + locked closure (Paper 0 row 10, observer-reading)",
        tier=3,
        epistemic="P",
        summary=(
            "The ledger basis grants observable status to the state-borne residue of its own "
            "transition log: commitment bookings are transition-toggled and residue-materialized "
            "(row 9 [P]); the residue is pin-indexed because the configuration space IS the product "
            "of per-interface holding bits (refined product space [P]); under Paper 0 v6.2.30 row 10 "
            "(observer-reading -- held content is evaluable content; an enacted operation's outcome "
            "is the post-state) the residue is evaluable at its billing interfaces; the locked part "
            "is unerasable (L_irr [P]). Fences asserted: residue cap (order-composed histories "
            "indistinguishable), h-invisibility (pre-enactment placements identical; separation only "
            "through enacted pin-death), activation-only events outside the log. Consequence "
            "executed: the two-placement ALIGNED/BORROWED fork separates at the first enacted "
            "pool-addressed withdrawal (pin-identified profiles class-disjoint; mirrored anonymized "
            "counts coincide). GRADE [P] over row 10 (zero-translation row consumption, falsifier "
            "(vii)), precedent class row-9/row-6. Billing-placement-blind: pin coordinate granted, "
            "pool coordinate enactment-supplied; no sector-row placement license; sin^2 theta_W "
            "stays [P_structural]."
        ),
        dependencies=["T_ledger_rent_excluded", "L_recoverability_is_orbit_reachability",
                      "L_local_removability", "L_irr",
                      "Paper0_row10_observer_reading"],
        cross_refs=["L_operational_completeness", "T_fusion_scope_dissolved",
                    "L_epsilon_star"],
        artifacts={
            "verdict": "YES at residue grade -- the basis was never blind, only silent; the silence = the state-function discipline",
            "or_landing": "OR landed as Paper 0 v6.2.30 row 10 per principal ruling 2026-06-12 (option 2: the row is the naming)",
            "join_split": "row grain pin x pool: PIN coordinate basis-granted (this theorem), POOL coordinate enactment-supplied (M2)",
            "separation": "ALIGNED survives the b1 withdrawal intact; BORROWED loses p1 -- A-constituted pin dead by B-pool enactment",
            "fences": "residue cap; activation-only events excluded; counterfactuals not granted; h not readable",
            "provenance": "two-placement enrichment + enforcement-reading cycles -> L2-observability adjudication v0.2 + cold audit REDUCES -> row 10 -> this consumption",
        },
    )


_CHECKS = {
    "L_operational_completeness": check_L_operational_completeness,
    "L_local_removability": check_L_local_removability,
    "L_recoverability_is_orbit_reachability": check_L_recoverability_is_orbit_reachability,
    "T_fusion_scope_dissolved": check_T_fusion_scope_dissolved,
    "T_ledger_rent_excluded": check_T_ledger_rent_excluded,
    "T_transition_log_residue_readable": check_T_transition_log_residue_readable,
}


def register(registry):
    registry.update(_CHECKS)
    return registry


def run_all():
    return {n: fn() for n, fn in _CHECKS.items()}


if __name__ == "__main__":
    for n, fn in _CHECKS.items():
        r = fn()
        print(f"{n}: passed={r.get('passed')} [{r.get('epistemic')}]")

# ---------------------------------------------------------------------------
# IE onboarding (Wave 7, v24.3.347).
# ---------------------------------------------------------------------------
IE_DECLARATIONS = (
    {
        "input_id": "foundation:operational_completeness_sandwich",
        "expect_export": False,
        "axis": "ROUTE",
        "claim_text": (
            "The access-partition arc's executable core: six checks, all machine- "
            "graded epistemic='P'. check_L_operational_completeness banks the "
            "SANDWICH: the operation set available to the observer at interface "
            "Gamma is EXACTLY the set of admissibility-data REMOVALS billed "
            "solely at Gamma, within global admissibility -- upper bound from "
            "L_loc (no cross-interface or simultaneous multi-interface "
            "modification), lower bound from L_irr's two necessity countermodels "
            "executed as check bodies (CM1 forces the footprint half, CM2 the "
            "single-billed-joint half), replacing what was previously prose-level "
            "license (CM2's old body was a literal True). The deletions-only "
            "narrowing is a rider inside the [P] statement itself: additions and "
            "general modifications are NOT claimed; the clause's strength is "
            "exactly its removals-only scope per the 2026-06-09 row-6 audit. "
            "check_L_local_removability banks the billing-locus converse of L_irr "
            "(a distinction's committed capacity is locally recoverable iff no "
            "cross-billed joint line contains it). The remaining four -- "
            "check_L_recoverability_is_orbit_reachability, "
            "check_T_fusion_scope_dissolved, check_T_ledger_rent_excluded, "
            "check_T_transition_log_residue_readable -- extend the ledger-line "
            "machinery, each at epistemic='P'. "
        ),
        "note": "Wave 7; flag: the _module_manifest.py:438 comment (v24.3.230 stamp) still reads '[P_structural] x2' for the first two checks, but the in-module machine fields are 'P' following the 2026-06-09 [P]-promotion audit cited in the module docstring -- fields win, manifest comment is stale.",
    },
)
